# 032 — Phase 03: Effects: ctx.llm, ctx.agent, ctx.subtask, ctx.gather, ctx.approve (in-process form) — execution summary

Status: done (independent verifier green, 1 fix round — test-only). No version bump — batched into the next manifest/UI bump (Risks 8).

## Ran
- Date: 2026-09-07. Repo `luna-plugins/plugins/plugin-playbooks`, branch `v2-runtime`. HEAD before `df5ed87` (plugin/02 summary), HEAD after `0411220` — one commit (amend of `096cdda`; the amend changed only `tests/test_v2_effects.py`), not pushed (`git ls-remote --heads origin v2-runtime` → empty). luna and dojoP untouched. Machine load average 160-316 throughout (other processes).
- Commit `0411220`: 7 files, +1343/-50 — `docs/v2.md` (+86), `plugin_playbooks/delegation.py` (25 lines), `plugin_playbooks/runner.py` (+3), `plugin_playbooks/v2/journal.py` (26), `plugin_playbooks/v2/loop.py` (398), `plugin_playbooks/v2/shim.py` (120), `tests/test_v2_effects.py` (new, 735 lines). Tests diff vs `df5ed87`: +735/-0 — no existing test deleted, weakened, skipped or xfailed. ` M uv.lock` left as found, not staged. Secret grep over the diff → 0 hits (matches were `import secrets`, the contextvar `token`, `total_tokens`).
- Commands (plugin venv, `.venv/bin/python -m pytest -q -p no:cacheprovider`, no `uv sync`):
  - `tests/test_v2_effects.py -rA` → 24 passed (17 tests, 24 cases with parametrization), 0 skipped — the real Seatbelt jail was available, `requires_jail()` did not skip. Whole file 22.65 s.
  - `tests/test_v2_contract_doc.py tests/test_no_spec_feature.py` → 23 passed (step 10). `tests/test_delegation.py` → 17 passed (step 3). `tests/test_v2_loop.py` green throughout.
  - Full suite `tests -q -rA` → 7 failed, 518 passed in 34.97 s; 525 collected = 501 at `df5ed87` (494 green + 7 red) + 24 new cases.
  - `--durations` on exit test 17: llm 1.25/1.23 s, agent 1.21/1.22 s (`_timeout=1`), subtask 3.23/3.23 s (`_timeout=3`).
- Independent verifier: re-ran every suite above at `0411220` with identical totals; 1 fix round (exit test 17's subtask cases flaked under load on a redundant child-journal assertion — see Deviations; no host code changed in the round).

## Results
Exit tests (`tests/test_v2_effects.py`, all `[real_jail]`, all green):
1. `test_llm_returns_dict_with_output_schema`: run `done`, value `{"x": 1}`, `output_schema == {"type": "object"}`, `purpose == "summarization"`.
2. `test_llm_returns_str_without_output`: value `"hi"`; entry `kind == "llm"`, `status == "done"`.
3. `test_llm_records_cost_and_billing_scope`: entry `cost_cents == 3`; `plugin_playbooks.runner._playbook_origin_scope` entered once with the playbook, the fake facade ran inside it; `agent=None` → run `failed`, error contains "requires an injected agent".
4. `test_agent_transcript_on_entry_and_nested_guard`: `active_run_id() == str(run.id)` inside `run_turn`, refusal `gate == "nested_playbook_run"`, entry `transcript` == the `_TranscriptFeed` reference list (tool event `ok is True`, then the thought), `tools == ["t"]`, `memory_write is False`, `conversation_id == run.report_to`.
5. `test_agent_aborted_answer_fails_loud`: entry `failed`, run `failed`, error contains "turn limit".
6. `test_gather_orders_results_and_runs_concurrently`: value `[slow, fast]`; seq 1 slow, seq 2 fast, both `done`; also pins step 2's done-when (one `kind: "gather"` exit carrying both pending effects in argument order).
7. `test_gather_raises_first_failure_after_all_settle`: value names `bad1`; `"slow-finished" in calls`; bad1 `failed_handled`, slow `done`, bad2 `failed_handled`; run `done`.
8. `test_subtask_returns_child_value_and_links_rows`: `{"n": 4}`; child `parent_run_id`, `trigger == f"subtask:{parent.id}"`, `is_test` inherited, `done`; parent entry `kind == "subtask"`, `child_run_id == str(child.id)`.
9. `test_subtask_cycle_guard_trips_with_existing_refusal`: exactly 2 run rows; error contains "would recurse" and both names; `a` and `b` `failed`.
10. `test_subtask_child_failure_is_catchable`: run `done`, value contains "boom" and the child id; entry `failed_handled` / `SubtaskFailed`; child `failed`.
11. `test_subtask_unknown_playbook_uses_v1_message`: `"Subtask playbook 'nope' not found"`, entry `failed`, run `failed`.
12. `test_approve_blocks_until_decided`: `"fast" not in calls` and row `running` while the card is pending; request `kind == "playbook_effect"`, `requested_by_plugin == "plugin-playbooks"`, `payload == {run_id: str(run.id), seq: 1, playbook, version}`, `presentation["changes"]` non-empty; approved → `done`, `set(entry["result"]) == APPROVE_RESULT_KEYS`.
13. `test_approve_rejected_raises_ctx_rejected_catchable[True|False]`: caught → `done`, value contains "no", entry `failed_handled`/`Rejected`; uncaught → `failed`, `error_type == "Rejected"`.
14. `test_approve_expired_raises_ctx_approval_expired[True|False]`: `get().status == "expired"` path and the no-`get` fallback (`reason == "ttl elapsed"`, `decided_by == "system"`) both raise `ctx.ApprovalExpired`, catchable → `done`.
15. `test_handled_tool_failure_is_failed_handled_and_run_completes`: entry 1 `failed_handled`, entry 2 `done`, run `done`.
16. `test_send_chat_message_conversation_rules_match_v1`: handler received `conversation_id == str(row.report_to)` (trigger `agent`, `_Ctx.current_conversation_id` set); journal `args == {"text": "hi"}` (see Deviations); `report_to=None`, `is_test=False` → entry `failed`/`ToolError`, run `failed`, error contains "this run has no chat to report to".
17. `test_effect_timeout_enforced_for_llm_agent_subtask[llm|agent|subtask × caught False|True]`: 6 green; entry `error.type == "EffectTimeout"`, message "timed out"; uncaught → run `failed`, `error_type == "EffectTimeout"`; caught → `done`, value `"late"`, entry `failed_handled`; subtask: child row `cancelled`, parent entry `child_run_id`, `"slow-finished"` never called, no child journal row left `in_flight`, every child effect row `failed`/`RunCancelled`.
- Existing suite: 7 red before, 7 red after, no flip — lifecycle ×3 (`test_publish_success_carries_verified_readback`, `test_approved_then_regated_same_payload_trips_loop_guard`, `test_manifest_set_does_not_flip_live`), runtime ×4 (`test_interrupted_run_survives_restart_instead_of_failing`, `test_wait_for_approval_actually_gates` — stays red as the v1 pin, its assertions pass on v2 in test 12, `test_wait_for_event_actually_waits`, `test_tool_step_timeout_is_enforced`). Stamps 0.47.0 unchanged (`pyproject.toml:3`, `plugin_playbooks/luna-plugin.toml:2`, `plugin_playbooks/__init__.py:621`); `tests/test_manifest_drift.py::test_version_stamps_agree` green.
- Load-only flakes seen in full runs at load 160-316, each passing in isolation 3/3, no change made: `test_wake_on_completion::test_slow_run_promises_wake_and_stamps_flag`, `test_delegation::test_main_turn_payloads_stay_small`, `test_delegation::test_slow_path_returns_running_then_status_polls_done` (fails identically at `df5ed87`).

## Deviations from this plan
- Step 1 name reconciliation: every phase 02 symbol named here existed at `df5ed87` (Risks 10 confirmed). `_perform` stayed an if/elif (`v2/loop.py:450-480`); unknown kinds now raise `_EffectFailure("effect kind … is not available in this version")`.
- Exit test 16: the journal row's `args` cannot carry the injected `conversation_id` — the shim's replay compares journaled args to the code's args and raises `JournalDivergence` otherwise (`v2/shim.py:402-408`). The test asserts the handler's received kwargs and the row's own args (`{"text": "hi"}`) instead.
- Exit test 17: `wait_for_run(timeout=90)` instead of 2.5 s (a loaded machine's jail segments exceed 2.5 s; the 1 s deadline is proven by the row's `EffectTimeout` and the measured 1.2 s durations); the subtask case uses `_timeout=3` so the child reaches its blocked tool before the deadline. Fix round 1 replaced an unconditional `cj[1]` RunCancelled assertion (never in the plan; racy — the child's first segment may not reach `ctx.tool("slow")` before the deadline under load) with the race-free invariant listed under Results 17; the stamp itself is pinned by `tests/test_v2_loop.py::test_cancel_run_v2_uncatchable`.
- Two host fixes beyond the phase text, both needed by the exit tests: `SegmentLoop.last_result` is set when a drive FINISHES (`loop.py:257`; a child finishes before its parent, so the parent's result is what stays), and `_journal_and_start` (`loop.py:371-377`) stamps the row `failed`/`RunCancelled` when a cancel lands between `append_in_flight` and the effect body (a subtask deadline cancelling its child otherwise left the child's row `in_flight`).
- Shim replay treats `failed_handled` like `failed` — re-raises the same class (`shim.py:416`), so the same `except` catches it on every replay; implied by the phase, not spelled out. `handled` is reported on every exit kind (`effect`, `gather`, `return`, `error`); a thrown replay failure counts as handled when the code reaches its next effect or returns (`shim.py:461-487`).
- Risks 2 confirmed: `_timeout` on `ctx.approve` → `ttl_seconds` (`int`), no host-side `wait_for`. Risks 3 as assumed: `conversation_id=run.report_to` (may be None), `presentation = {eyebrow: "Playbook approval", headline, explanation, changes: [{label, kind: "text", text: <show rendered>}]}`. Risks 4 kept: `_aborted` answer → `EffectError(error)` with the transcript on the failed row. Risks 5: `returns=None` → whole value; `returns=[keys]` projects a dict, non-dict or missing key → `SubtaskFailed`. Risks 6: pblang child → `EffectError("subtask target '<name>' is not a v2 playbook")` (`loop.py:612-613`); the child runs its live version. Risks 7 as read: chain check with "would recurse" wording, no depth limit. Risks 12 confirmed: host-owned deadline via `asyncio.wait({task}, timeout)`, flag + `task.cancel()`, child row `cancelled`, `EffectTimeout` raised from the flag (`loop.py:631-660`); the real facade was not checked for swallowed cancellation (fakes only).
- `_timeout` default for llm/agent/subtask: `_timeout_for` (`loop.py:482-486`) reads `options["_timeout"]` else `DEFAULT_TIMEOUTS[kind]` (subtask None = unbounded). `_retry` applies to `tool`/`llm`/`agent` only (`loop.py:398`).
- `_effect_subtask` runs `start_run` in its own task also without a `_timeout`, and on a parent cancel cancels the child task before re-raising.
- No version bump — batched into the next manifest/UI bump.
- Plan files under later phases were not edited by the executor; the revisions below are this summary's.

## Learned
- Landed constructor: `SegmentLoop(session_factory, tools, events, ctx, journal, *, segment_timeout=60, max_effects=MAX_EFFECTS, agent=None, start_run=None)` (`loop.py:167-171`); `PlaybookRunner.__init__` passes `agent=agent, start_run=self.start_run` (`runner.py:307-311`). `LoopResult` still `(value, segments, segment_latency_ms)` — no `parked` field. Per-run state: `SegmentLoop._runs: dict[str, _RunState(name, version, chain)]` (`loop.py:101-109`, :188, set at :208 from the parent's chain when `run.parent_run_id` is a live run; popped in `finally`) and `SegmentLoop._values` (child return values keyed by run id, written at `return` when `parent_id is not None`, popped by the parent's `_effect_subtask`). Both are in-memory: plugin/06's resume must rebuild the chain from `playbook_runs.parent_run_id`.
- `JournalStore` Protocol now 8 methods: `complete(run_id, seq, result, attempts, ms, extra=None)`, `fail(run_id, seq, error_type, message, attempts, extra=None)`, new `mark_handled(run_id, seqs)` (re-stamps only rows whose status is `failed`; `journal.py:142-146`); `extra` merges `cost_cents` (llm/agent), `transcript` (agent, also on failed rows), `child_run_id` (subtask, also on failed/timeout rows) onto the row. Plugin/06's table needs columns `cost_cents` (Integer nullable), `transcript` (JSON), `child_run_id` (UUID nullable) and the `failed_handled` status.
- Host error classes (`loop.py:66-99`): `_EffectFailure(message, extra=None)` → `EffectError`; `_ToolError`, `_EffectTimeout`, `_Rejected`, `_ApprovalExpired`, `_SubtaskFailed` (error types `ToolError`, `EffectTimeout`, `Rejected`, `ApprovalExpired`, `SubtaskFailed`). Dispatch `_perform(run, seq, kind, key, name, args, options) -> (journal_result, step_outputs, extra_fields)` (`loop.py:450-480`); handlers `_effect_llm` :528, `_effect_agent` :552, `_effect_subtask` :593-683, `_effect_approve` :685-743, `_approval_expired` :745-760, `_perform_tool` :762; `_gather` :328-342 (`return_exceptions=True`, first exception in argument order re-raised after all settle); step helpers `_create_step` :811, `_update_step_retry` :824, `_complete_step` :833.
- Approve request shape plugin/07 must keep (`loop.py:711-721`): `approvals.request(kind="playbook_effect", summary=<headline: first line of show>, payload={"run_id": str(run.id), "seq": int, "playbook": name, "version": int}, requested_by_plugin="plugin-playbooks", risk_level="medium", conversation_id=run.report_to, presentation={eyebrow, headline, explanation, changes}, ttl_seconds=int(_timeout) | None)`; `approvals` is `getattr(self._ctx, "approval", None)`, missing → `EffectError`. Result `{"approved": True, "request_id": str|None, "reason", "decided_by"}` (`loop.py:735`) — `APPROVE_RESULT_KEYS`, `request_id` not `approval_id` (plugin/07 Risks 19 confirmed). A raising engine → `EffectError("approval request failed: …")`. Expiry: `get(request_id).status == "expired"` (sync or async `get`), else the `ttl elapsed`/`system` fallback.
- Subtask: child created by `start_run(target, inputs=, trigger=f"subtask:{run.id}", parent_run_id=run.id, is_test=)` in its own task (`loop.py:626-632`); ANY non-`done` child status (`failed`, `cancelled`, and a future `parked`) raises `SubtaskFailed("subtask '<name>' (run <id>) <status>: <error>")` — plugin/07's parked-child question (its Risks 10) lands on this branch. Journal `result` for a subtask row is the (projected) value; step outputs `{"subtask", "run_id", "result"}`.
- Shim (`v2/shim.py`): `_pb_Ctx.llm(prompt, *, output=None, purpose=None, model=None, system=None, **options)` :271, `agent(prompt, *, output=None, tools=None, **options)` :279, `subtask(playbook, inputs=None, *, returns=None, **options)` :286, `approve(*, show, **options)` :293, `gather(*handles)` :298 (rejects non-handles with `EffectError`), `wait_event` still `_pb_NotWired` at :309-310; `_pb_GatherRequest`/`_pb_GatherAwaitable` :153-172; `_pb_handled` :322; replay `_pb_serve` :391-436 (`failed`/`failed_handled` → raise the mapped class, `in_flight`/`timed_out_unknown` → `OutcomeUnknown`); gather exit `{"kind": "gather", "seq": <first pending seq>, "effects": [...], "handled": [...]}` :499-500. Every exit payload carries `handled`.
- `delegation.py`: `_TranscriptFeed` :443-535 (`events`, `steps_used`, `_append`, `_map_event`, `handle`, no-op `maybe_flush` :458), `_EventFeed(_TranscriptFeed)` :536 keeps the DB `maybe_flush` :545; `_LIVE_FEEDS: dict[uuid.UUID, _EventFeed]` :652. Transcript event dicts: `{kind, label, detail, ok, ts, ms}`.
- `docs/v2.md`: §2.1-2.6 (:115-195) per effect (signature, return rule, exceptions, journal fields), §2.6 caught failures → `failed_handled`; §6 status set `in_flight | done | failed | failed_handled | timed_out_unknown | parked` (:270) and the field bullet (:292-293).
- Current `runner.py` anchors (after phase 03): `SegmentLoop(...)` :307-311, `start_run` :312-332, `start_run_background` :334, `sweep_orphaned_runs` :372, `_create_run` :434, `_drive_run` :499 (`PlaybookDef.model_validate` :525 still precedes the v2 branch :544-548; `except V2RunError` :560-568; `except asyncio.CancelledError` :580-586), `dry_run` :633 (`model_validate` :645), `cancel_run` :690-707, `_resolve_vault_refs` :835, `_create_step_run` :1399, `_complete_step` :1445, `_complete_run` :1466 (`_active_run_id.set(None)` :1525). `__init__.py:748` `agent=ctx.agent`.
- Suite baseline for plugin/04: 525 collected = 518 green + 7 red at `0411220`; stamps 0.47.0.
- Test harness reuse worked by import (`from test_delegation import FakeAgent, …`, `from test_manifest_flow import _Agent`, `from test_repro_fixplaybooks_lifecycle import _Approvals, _Ctx, _Decision`) — no verbatim copies; `_Ctx` subclass adds `current_conversation_id = None`, `vault = None`.
- On this machine at load ≥160, a jail segment can take several seconds; timing assertions in later phases should bound waits generously and assert the deadline through the journal row.

## Revised
- `phases/04-lifecycle-corrections/PLAN.md`: `_drive_run` bullet line refs (:499, v2 branch :544-548, `except V2RunError` :560-568, `model_validate` :525); Step 1 baseline (525 = 518 + 7 at `0411220`); Risks 8 (phase 03 stamps 0.47.0 at `0411220`, confirmed).
- `phases/05-dry-run-skill-and-go-no-go/PLAN.md`: Scope item 2 (constructor gained `agent=`/`start_run=`; step helpers now `v2/loop.py:811-848`); Risks 14 confirmed (live keys `approved, request_id, reason, decided_by`, `loop.py:735`).
- `phases/06-durable-journal-and-resume/PLAN.md`: column brief gains `cost_cents` and `child_run_id`; runner line refs (`_complete_run` :1466, `_drive_run` :499, `cancel_run` :690-707, sweep :372); Cross-repo plugin/02 line (`JournalStore` is 8 methods after phase 03, `complete`/`fail` take `extra=`); plugin/03 line (chain lives in `SegmentLoop._runs`, child values in `_values`, both memory-only); Risks 9 (the shim reports every proceeded-past replay failure incl. `OutcomeUnknown` in `handled`, but `mark_handled` re-stamps only `failed` rows).
- `phases/07-parked-runs/PLAN.md`: shim `wait_event` stub now `shim.py:309-310`; loop bullet (`LoopResult` confirmed without `parked` after phase 03; `_gather` is `return_exceptions=True`); runner bullet anchors (`_drive_run` :499, `cancel_run` :690-707, sweep :372); Risks 10 (`start_run` :312-332; phase 03's `_effect_subtask` raises `SubtaskFailed` for any non-`done` child status, so a parked child fails the parent's effect until this phase decides); Risks 19 confirmed at phase 03.
- `phases/08-lifecycle-integration-and-parity/PLAN.md`: `ctx.subtask` bullet and Risks 7 — phase 03 refuses a pblang child (`EffectError("subtask target '<name>' is not a v2 playbook")`, `loop.py:612-613`), it never returned the v1 dict; the pblang-child behaviour is this phase's decision.
- Repo `PLAN.md`: no correction requested by this phase file; none made.
