# 032 — Phase 02: In-jail shim, host segment loop, ctx.tool/now/random/log, error contract, hash-seed pin — execution summary

Status: done (independent verifier green, 0 fix rounds). Hash-seed option A taken; no change in plugin-inline-code-run.

## Ran
- Date: 2026-09-07. Repo `luna-plugins/plugins/plugin-playbooks`, branch `v2-runtime`. HEAD before `9b1a6d1` (plugin/01 summary), HEAD after `12e425f` — one commit, not pushed (`git ls-remote --heads origin v2-runtime` → empty). luna-fixer: `687e4ad` → `ea38242`, local only (`inline-code-run-plan.md` +46/-2: `Status: withdrawn`, §6 filled). dojoP untouched.
- Commit `12e425f`: 11 files, +2475/-56 — `docs/v2.md`, `plugin_playbooks/__init__.py`, `plugin_playbooks/models.py`, `plugin_playbooks/runner.py`, `plugin_playbooks/v2/__init__.py`, `plugin_playbooks/v2/journal.py` (new), `plugin_playbooks/v2/loop.py` (new), `plugin_playbooks/v2/shim.py` (new), `pyproject.toml`, `tests/_jail.py` (new), `tests/test_v2_loop.py` (new). Tests diff +1215/-0: no existing test deleted, weakened, skipped or xfailed.
- The code pre-existed uncommitted from an interrupted executor run; this run read it against the plan, ran every proof, measured, filled the decision record and committed. ` M uv.lock` left as found, not staged.
- Commands (plugin venv `.venv/bin/python`, no `uv sync`):
  - `.venv/bin/python -m pytest tests/test_v2_loop.py -q` → 26 passed (10 `real_jail` on Seatbelt, 16 jail-free).
  - `.venv/bin/python -m pytest tests/test_v2_loop.py -q -m real_jail -s` → 10 passed, 16 deselected; `v2-hashseed` and `v2-latency` lines below.
  - `.venv/bin/python -m pytest tests/test_v2_contract_doc.py tests/test_manifest_drift.py tests/test_v2_checker.py -q` → 94 passed.
  - `.venv/bin/python -m pytest tests -q` → 7 failed, 494 passed (501 collected = 475 at `9b1a6d1` + 26 new); `scratchpad/full_suite.txt`.
  - `uvx ruff check --select F401 plugin_playbooks tests` → clean. Secret grep over the diff (`lsk_`, `sk-`, `token`, `.env`) → 0 hits.
  - bwrap evidence: `scratchpad/bwrap_run.sh` — `docker run --privileged` on the fleet image `python:3.12-slim` (Debian 13, Python 3.12.14, aarch64, bubblewrap 0.12.0), repo and `~/.luna/managed_plugins/plugin_inline_code_run` (0.4.0) mounted read-only, `pytest tests/test_v2_loop.py -m real_jail -s` inside → 10 passed, 16 deselected; log `scratchpad/bwrap_run.log`. No remote machine touched.
  - execve delta: `scratchpad/execve_delta.py` (5 runs each through `tests/_jail.py::real_code_run`).
- Independent verifier: same suites re-run green (26/26; 10 `real_jail`; full 494 green + 7 red); its `v2-hashseed` on Seatbelt `h=-1454083024660927584` (different seed, same `isolated=0` / env); its `v2-latency` medians 364/411/350/388 host ms. 0 fix rounds.

## Results
Hash-seed decision (Step 7) — option A, both backends green on `test_set_iteration_replays_identically_across_spawns`:
- Seatbelt (this machine): `v2-hashseed backend=seatbelt isolated=0 h=-3944133801732233452 env=['PYTHONDONTWRITEBYTECODE', 'PYTHONHASHSEED']`.
- bwrap (fleet image via Docker): `v2-hashseed backend=bwrap isolated=0 h=-1097187551971987524 env=['PYTHONDONTWRITEBYTECODE', 'PYTHONHASHSEED']`.
- inline-code-run-plan §3 items: (1) bwrap re-exec works at the ro-bound interpreter path under `--new-session`/`--die-with-parent`; (2) Seatbelt `(allow process-exec)` suffices, `abspath(sys.argv[0])` resolves `main.py` in the run dir; (3) cost: plain `-I` `return 1` median 131 ms vs shim median 255 ms on Seatbelt → ~124 ms per segment for the re-exec + compile; (4) loud path: three `HashSeedPinFailed` returns in `shim.py` (empty `sys.executable`, `OSError` from `execve`, `execve` returned) — in code, not test-exercised (no way to make `execve` fail inside the real jail without patching the install); (5) env: in-jail `sorted(PYTHON*) == ["PYTHONDONTWRITEBYTECODE", "PYTHONHASHSEED"]` on both backends. §4 transport: zero-change route — ONE `outputs/result.json`, `kind: effect | return | error` (`gather` reserved for plugin/03), exit 0 in every shim-handled case; `progress.json` reaches production only via a storage provider.

Latency (Step 8, `test_segment_latency_recorded`, `n=4` segments of the 3-effect run):
- Seatbelt, 5 runs: median_host_ms 306 / 257 / 258 / 294 / 284; max_host_ms 1124 / 339 / 332 / 296 / 310; median_jail_ms 292 / 246 / 244 / 264 / 268 (an earlier cold run: 82 / 80).
- bwrap (fleet image, re-exec included), 5 runs: median_host_ms 86 / 150 / 100 / 121 / 92; median_jail_ms 83 / 132 / 92 / 116 / 86; max_host_ms ≤ 186.
- The ~100 ms budget (master §5) holds on the fleet backend; Seatbelt on this machine is ~2.5× that (Risks 6: recorded, not a reason for B).

Exit tests (`tests/test_v2_loop.py`, all green):
- `test_three_effects_complete_through_jail_identical_journal` [real_jail]: `done`, 4 segments, 3 step rows `outputs == {"tool", "result"}` with `step_id` `a#1`/`b#1`/`c#1`; `strip(j1) == strip(j2)`.
- `test_set_iteration_replays_identically_across_spawns` [real_jail]: 2 runs, identical `list(s)` arg in every spawn; `isolated == 0`, `h` identical, env as above (8 spawns — see Deviations).
- `test_vault_ref_raw_in_journal_resolved_at_execution`: handler sees the secret; journal, step `inputs`, events carry `vault:my_key` only.
- `test_effect_timeout_enforced[explicit|default]`: `failed` within 2.5 s, step error "timed out", entry `failed`/`EffectTimeout`; the default case via `monkeypatch` of `plugin_playbooks.v2.loop.DEFAULT_TIMEOUTS`.
- `test_call_site_ids_not_tool_name_counting` (scripted) and `_real_jail` twin: tuples `("write_a",1,"file_write")`, `("write_b",1,"file_write")`, `("file_write",1,"file_write")`; loop `("touch",1)`, `("touch",2)`; rows `write_a#1`, `write_b#1`, `file_write#1`, `touch#1`, `touch#2`.
- `test_tool_effect_runs_in_billing_scope`: `plugin_playbooks.runner._playbook_origin_scope` entered once with the playbook, handler runs inside it — the v2 branch sits inside the `with` (`runner.py` `_drive_run`, now :544).
- `test_retry_single_entry_with_attempts`: one entry, `len(attempts) == 3`, `retry_count == 2`, run `done`.
- `test_max_effects_trips_loudly` [real_jail]: `MaxEffectsExceeded`, `row.error` names `touch#6`, `5` and `MAX_EFFECTS`.
- `test_keyerror_in_loop_body_reports_line_effect_locals` [real_jail]: `KeyError`, `playbook_line` = the `r["missing"]` line, `last_completed_effect == {"seq": 3, "id": "touch#2", "kind": "tool"}`, locals `i`/`r`, frames `playbook:<name>@v1`, `row.error.startswith("line 5:")`, `failed_at` set.
- `test_error_payload_persisted_on_run_row`: four columns persisted; `playbook.run.completed` carries `status == "failed"` and `error`.
- `test_segment_timeout_names_last_effect` [real_jail] + `_scripted`: `SegmentTimeout`, `row.error` contains "after effect fetch#1", failed within 4 s.
- `test_now_random_replayed_not_redrawn` [real_jail]: tools `a`/`b` record identical `r`/`t`; entries `("r",1,"random")` and `("t",1,"now")` exactly once each; 5 segments; `t` is an aware `datetime` in every segment (see Deviations).
- `test_cancel_run_v2_uncatchable`: `cancelled` via the task path; entry 1 `failed`/`RunCancelled`; exactly one `code_run` call; completed event `status == "cancelled"`; step error contains "cancelled".
- `test_completion_emit_sees_no_active_run`: `active_run_id()` and `_nested_run_refusal()` both `None` on `playbook.run.completed`; `active_run_id() == str(run.id)` on `playbook.step.started`.
- `test_print_never_appears` [real_jail]: `LEAK-7f3a` absent from journal, rows, events, payload `stdout`.
- `test_ctx_log_entries_in_trace`: two `log` rows with `outputs["message"]` in order.
- `test_journal_divergence_typed_error` [real_jail]: `JournalDivergence`; message contains "set iteration", "code edited under a run", "non-journaled randomness" (`shim.py:193-198`).
- `test_journal_first_then_execute`: `in_flight` row with `idempotency_key == f"{run.id}:1"` visible inside the handler; `done` afterwards.
- `test_active_run_id_and_nested_refusal_during_effect`: `active_run_id() == str(run.id)` and the `nested_playbook_run` refusal inside the handler; `None` after.
- `test_v2_run_swept_on_restart_says_so`: `failed`, `error` contains "interrupted", `error_type == "Interrupted"`.
- `test_run_error_columns_migrate`: exactly the four columns added; second call adds none.
- `test_memory_journal_store_roundtrip`: shapes per `docs/v2.md` §6.
- `test_segment_latency_recorded` [real_jail]: 4 pairs `> 0`; prints the `v2-latency` line.
- Existing suite: 7 red before, 7 red after, no flip — lifecycle ×3 (`test_publish_success_carries_verified_readback`, `test_approved_then_regated_same_payload_trips_loop_guard`, `test_manifest_set_does_not_flip_live`), runtime ×4 (`test_interrupted_run_survives_restart_instead_of_failing`, `test_wait_for_approval_actually_gates`, `test_wait_for_event_actually_waits`, `test_tool_step_timeout_is_enforced` — stays red as the v1 pin; its assertions pass on v2 in `test_effect_timeout_enforced`). Stamps 0.47.0 unchanged (`pyproject.toml:3`, `luna-plugin.toml:2`, `__init__.py`); `tests/test_manifest_drift.py` green, tables 10.

## Deviations from this plan
- Code was already on disk, uncommitted, from an interrupted executor run; it was verified against the plan and committed rather than rewritten.
- Plan self-contradictions resolved in favour of the Scope rule (call-site id = assignment target): `test_now_random_replayed_not_redrawn` asserts `("r", 1, "random")` / `("t", 1, "now")` (not `("random", 1)`) and 5 segments (random, now, a, b, return — not 3); `test_three_effects_…` step ids are `a#1`/`b#1`/`c#1`; the set-iteration test makes 8 spawns (two runs × (3 effects + return)), not 6.
- `dir(ctx)` includes `wait_event` (raising `EffectError("ctx.wait_event is not available in this version")`, `shim.py:259-260`) in addition to `AVAILABLE_EFFECTS | CTX_EXCEPTIONS | CTX_UNCATCHABLE`; phase 01's equality check is over the effect/exception names, and the checker still rejects `wait_event` (R10).
- bwrap evidence gathered on the fleet image through local Docker (`--privileged`), not on a `vaselin-*` agent; Risks 5's "not done on Seatbelt evidence alone" is met.
- `runner.resolve_vault_refs(vault, args, *, step_id)` hoisted to module level (`runner.py:175`) so the loop shares it; the `_resolve_vault_refs` method (`:832`) delegates to it. `JournalStore` gained `drop(run_id)` (7 methods; `drive()` awaits it in `finally`; documented as a no-op for durable stores). The effect payload carries `call_site_id`, `occurrence`, `name`, `options` beside `seq`/`id`/`effect_kind`/`args` (`docs/v2.md` §6 kind table).
- The shim drives `run()` with `coro.send`/`coro.throw` directly — no `asyncio.run` in-jail; effect methods are sync and return awaitables, so `ctx.gather(ctx.tool(...), ...)` resolves sites at call time (plugin/03).
- `_perform` is an if/elif on kind (`loop.py:386-404`), not a dispatch table; unknown kinds raise `_EffectFailure("effect kind … is not wired until plugin/03")`. `_gather(run, res)` is implemented (`loop.py:277-290`: journals every `res["effects"]` in order, then `asyncio.gather(..., return_exceptions=False)`); the shim never emits `gather` yet.
- The loop has its own step-row helpers `_create_step(run_id, step_id, step_kind)` / `_update_step_retry` / `_complete_step(step_run_id, status, outputs=, error=, inputs=)` (`loop.py:455-493`); v1's `_create_step_run(run_id, step: StepDef)` was not generalized.
- The `send_chat_message` conversation-id rule is implemented in `_perform_tool` (`loop.py:406-425`): inherits `run.report_to`, else `_ToolError("effect '<key>': this run has no chat to report to — scheduled/background runs do not deliver to the ops chat. Give send_chat_message an explicit conversation_id.")` unless `run.is_test`. Phase 03 step 9 becomes pin-only.
- `run.error` one-liner is `line <n>: <src> → <error_type>: <message> after effect <id>#<n>` (message included; "before any effect" when none) — `loop.py:106-121`.
- inline-code-run-plan status set to `withdrawn` (its preamble rule: option A → no change in plugin-inline-code-run).
- Repo `PLAN.md` Risk 1 corrected: the file existed before this phase; §6 was filled, nothing created.
- Line numbers in this phase file shifted (phase 00/01 landed on top); every citation was located by symbol: `_playbook_origin_scope` :173 → :220, v2 branch :489 → :541-548, `_complete_run` :1427 → :1463, `cancel_run` :614-631 → :687-704, `_drive_run` :439 → :496, sweep :317 → :369.
- No version bump (batched into plugin/04).

## Learned
- Landed signatures: `SegmentLoop(session_factory, tools, events, ctx, journal, *, segment_timeout=60, max_effects=MAX_EFFECTS)`; `drive(run, playbook, inputs) -> LoopResult(value, segments, segment_latency_ms: list[tuple[int, int]])` — no `mode=` kwarg; `self.journal`, `self.last_result` (tests read `runner._v2.last_result`). Reachable as `PlaybookRunner._v2` (`runner.py:305-307`, built with `MemoryJournalStore()`).
- `V2RunError(error, error_type, traceback=None, failed_at=None)` is what `drive()` raises; `_drive_run` maps it to `_complete_run(run.id, "failed", error=, error_type=, traceback=, failed_at=)` (`runner.py:557-565`). `_complete_run(self, run_id, status, error=None, error_type=None, traceback=None, failed_at=None)` at `:1463`.
- Host error classes in `loop.py`: `_EffectFailure` (error_type `EffectError`), `_ToolError`, `_EffectTimeout`, `_RETRYABLE`, `SegmentTimeout`, `ShimFailure`; `_parse_retry` accepts `None|bool|int|{"attempts","backoff"}`; backoff `min(backoff * 2**(n-1), 60)`.
- `_execute_and_finish` sets `_active_run_id` per effect (`loop.py:337`), appends `attempts` only AFTER an attempt returns, then `journal.complete`/`fail`, `_complete_step`, `playbook.step.failed {run_id, step_id, error, retry_count}` / `playbook.step.completed {run_id, step_id, outputs}` (`:361-373`); `playbook.step.started {run_id, step_id, step_kind}` in `_journal_and_start` (`:311`). Only `except asyncio.CancelledError` writes the journal (`RunCancelled`) — no `except BaseException`; a simulated death leaves the `in_flight` row with `attempts == []`.
- `JournalStore` Protocol (7): `start`, `append_in_flight -> int`, `complete(run_id, seq, result, attempts, ms)`, `fail(run_id, seq, error_type, message, attempts)`, `read`, `entry0`, `drop`. `MemoryJournalStore(*, keep_completed=False)`; `fail` sets `error = {"type", "message"}`. `make_entry0(*, hash_seed, inputs, playbook, version, max_effects, mode="real", fmt="python")`; `make_effect_entry(*, run_id, seq, kind, id, occurrence, name, args, dry=False)`.
- Shim: `SHIM_SOURCE` is the whole `code` argument; the playbook source rides in the envelope `{playbook, version, source, hash_seed, max_effects, call_sites, journal}`; `_pb_EFFECT_KINDS = (tool, llm, agent, subtask, gather, approve, now, random, log)`; exception family at `shim.py:101-116`; `llm/agent/subtask/gather/approve` are `_pb_NotWired` (`:244-257`), `wait_event` likewise (`:259-260`); replay of `in_flight`/`timed_out_unknown` → `OutcomeUnknown` (`_pb_serve`); `MaxEffectsExceeded` message "effect <key> would be effect #<seq>, past the cap of <n> effects per run (MAX_EFFECTS)".
- Effect exit payload: `{"kind": "effect", seq, id: "<id>#<n>", call_site_id, occurrence, effect_kind, name, args, options}`; `gather` payload `{seq, effects: [...]}`. `options` = `{_timeout, _retry}` when given.
- `tests/_jail.py`: `jail_available()` :65, `real_jail = pytest.mark.real_jail` :77, `requires_jail()` :80 (skipif), `real_code_run(tmp_root)` :87; `pyproject.toml:16` carries the `real_jail` marker.
- `DEFAULT_TIMEOUTS = {"tool": 120, "llm": 300, "agent": 900, "subtask": None}` (`v2/__init__.py:44`); `v2/__init__.py` exports `JournalStore, MemoryJournalStore, SegmentLoop, SHIM_SOURCE`. `APPROVE_RESULT_KEYS` comment already says `approval_id` is reserved for `parked_on`, `request_id` is the result key (plugin/07 Risks 19 resolved).
- `docs/v2.md` has 12 `## ` sections; §6 carries entry 0 (:181), the envelope (:221) and the kind table; §11 row (:393) states option A and the kinds.
- Current `runner.py` anchors: `_playbook_origin_scope` :220, `PlaybookRunner.__init__` :283, `sweep_orphaned_runs` :369, `_create_run` :431, `_drive_run` :496 (`PlaybookDef.model_validate` :522 still precedes the v2 branch :541-548 — plugin/04 hoists), `dry_run` :630 (`model_validate` :642), `cancel_run` :687-704, `_resolve_vault_refs` :832, `_create_step_run` :1396, `_complete_step` :1442, `_complete_run` :1463 (`_active_run_id.set(None)` :1522, completed emit :1524). `models.py` run columns :177-186; `__init__.py` `_COLUMN_MIGRATIONS` rows :37-40.
- Suite baseline for plugin/03: 501 collected = 494 green + 7 red.

## Revised
- `phases/03-llm-agent-subtask-gather-approve/PLAN.md`: header HEAD; Scope `ctx.llm` billing-scope bullet (the v2 branch IS inside `_playbook_origin_scope`; no `v2/loop.py` import, no per-effect wrapper); gather bullet (`_gather` exists, payload keys, `return_exceptions=False` today); `send_chat_message` bullet and Step 9 (already in `_perform_tool`, pin only); Step 1/4 (if/elif dispatch, `_active_run_id` per effect done); exit test 3 (monkeypatch `plugin_playbooks.runner._playbook_origin_scope`, once per run); Existing suite counts (494 + 7 = 501); Risks 10 names confirmed; runner line refs.
- `phases/04-lifecycle-corrections/PLAN.md`: `_drive_run` bullet (branch at `runner.py:541-548`, `model_validate` :522); `_coerce_inputs` bullet (`_complete_run` keyword form confirmed, :1463); error-surfacing one-liner form (`KeyError: 'items'` — message included); exit test `startswith`; Risks 8 (stamp 0.47.0 after phase 02 confirmed).
- `phases/05-dry-run-skill-and-go-no-go/PLAN.md`: Scope item 2 (no `mode=` at HEAD; `LoopResult` fields; loop's own `_create_step`; `make_effect_entry(dry=)` and `MemoryJournalStore(keep_completed=)` exist; `drop` in `finally`); harness sentence (`<kind>`, not `<kind>#<n>`); Risks 5 and 7 confirmed (single-file shim; loop as `runner._v2`).
- `phases/06-durable-journal-and-resume/PLAN.md`: `_resume_run` line refs; harness (`_jail.py`/marker exist; only `CancelledError` caught — confirmed); Cross-repo plugin/02 line (`drop` must be a no-op on `DbJournalStore`; `OutcomeUnknown` replay confirmed at `shim.py`); Risks 2 and 16 (`attempts == []` at death → tighten to `== 0`).
- `phases/07-parked-runs/PLAN.md`: shim bullet (`wait_event` already an attribute raising `EffectError`, `shim.py:259-260`; `dir(ctx)` includes it); Step 4 check; harness (markers present at `pyproject.toml:16`); `_fail_loudly` signature confirmed; `LoopResult` has no `parked` field yet; Risks 19 resolved (`request_id`).
- Repo `PLAN.md` Risk 1: the file existed; phase 02 filled its §6 (status `withdrawn`).
- luna-fixer `plans/2026-09-06-fix-playbooks/inline-code-run-plan.md` §6 filled at `ea38242` (recorded here; `phases/M1-authoring-surface/execution_summary.md` copy is the M1 orchestrator's step).
