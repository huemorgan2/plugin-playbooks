# 032 — Phase 11 — execution summary

Status of this phase: in progress — code landed (parts a + b), keyhole gate pending (dojop/02 appends its verdict block below).

## Ran

Ran: 2026-09-08, Claude (Fable 5.1) as the luna-fixer M4 executor, in two parts on plugin-playbooks `v2-runtime`. Entry `a8ec656` (plans; code `da264d0` = plugin/10; stamps 0.54.0 ×3; bundle pair `index-BmqqWkXy.js` / `index-N9IomYre.css`; suite 705 collected = 698 green + 7 red pins; `npm test` 20 files / 149 tests). Local commits only, nothing pushed, no `vaselin-*` agent used, luna and dojoP not touched:
- Part a `aa17ef1` "032 phase 11 (part a): writer identity, author stamping, candidate-conflict guard, v2 delegate prompt" (Steps 1-6; stamps stayed 0.54.0): `plugin_playbooks/delegation.py` (`_delegation_id` ContextVar :55, `writer_identity()` :60, `_referenced_tools` python branch :166, `_PROMPT_TAIL_V2` :229, `V2_PROMPT_MARKER = "Format: python (v2)"` :238, `_prompt_format` :241, `_delegate_prompt(task, pb, *, format=None)` :247, `_drive_delegation` sets the token :756 / resets in `finally` :809, `playbook_agent` ToolDef description), `plugin_playbooks/versioning.py` (`author_label` :110, `candidate_conflict(session, p, author)` :124, `conflict_message` :143), `plugin_playbooks/agent_tools.py` (`_propose` stamps + guard :593-600, `_edit_impl(replace_candidate=False)` :2676 with `author = writer_identity()` :2682, READ header `candidate_author` + `conflict` + instruction prefix :2743-2752, WRITE guard after the version-race check and before `_check_ticket(consume=True)` :2971-2986, `_playbook_edit` param :3076/:3082, `_EDIT_PAYLOAD_PROPS.replace_candidate` :3103, `_manifest_set` stamp :3170), `plugin_playbooks/__init__.py:671` `_DELEGATION_SKILL_BODY`, `tests/test_v2_delegation.py` (new), `tests/test_delegate_prompt.py` (explicit `format="pblang"`). Suite after part a: 718 collected = 711 green + the 7 pins.
- Part b `0ca8b5f` "032 phase 11 (part b): versions UI author labels, end-to-end script, manifest — 0.55.0" (Steps 7, 8, 9) — 8 files, +382/−22: `ui-src/src/playbooks/VersionsTab.tsx` (`authorLabel` exported, `system` + `delegation:<id>` → "delegation <8 chars>", the row span `data-testid="version-author-<n>"` with `title={v.author}`), `ui-src/src/playbooks/__tests__/VersionsTab.test.tsx` (`renders author labels`), the bundle (`plugin_playbooks/ui/index.html`, `ui/assets/index-BmqqWkXy.js` → `index-CFve1-99.js`; `index-N9IomYre.css` unchanged — no CSS touched, same hash), `tests/test_v2_end_to_end.py` (new, 2 tests), the three stamps 0.54.0 → **0.55.0** (`pyproject.toml:3`, `plugin_playbooks/luna-plugin.toml:2`, `plugin_playbooks/__init__.py:728`), `plugin_playbooks/luna-plugin.toml:190` `playbook_agent` description re-written by hand to the ToolDef text (26 tools / 11 tables / 3 skills unchanged).
- Docs commit on top (this file, phase 12 PLAN.md dated line, this phase's PLAN.md status line `in progress — gate pending (dojop/02)`).
- ` M uv.lock` pre-existing: never staged, never discarded. `ui-src/tsconfig.tsbuildinfo` not regenerated this time (unchanged).

Commands (plugin repo): `.venv/bin/python -m pytest -p no:cacheprovider -q -rfEsx tests` (full suite, twice: 82.23 s / 81.13 s); `.venv/bin/python -m pytest -p no:cacheprovider -q tests/test_loader_style_import.py tests/test_manifest_drift.py tests/test_v2_end_to_end.py tests/test_v2_delegation.py tests/test_delegation.py` (38 green) before the commit; `uvx ruff check --select F401 plugin_playbooks tests` (clean); `cd ui-src && npm test && npm run build` (vitest 4.1.9, 20 files / 150 tests; build 2.33 s, one JS chunk 513.54 kB).

Step 1 baseline / re-mapped anchors: see part a's section in the M4 notes and the "Ran" bullet above (delegation.py citations of the plan's 8c31a60 moved by phase 00's deletions and phases 04/09; symbols are the anchor).

## Results (exit tests)

- `tests/test_v2_delegation.py` (part a): `test_conflict_guard_names_the_author`, `test_same_author_resave_moves_pointer`, `test_replace_candidate_is_explicit`, `test_propose_recreate_refuses_foreign_candidate`, `test_delegated_edit_stamps_delegation_author` (row `author == "delegation:<id>"`; `playbook_versions`, `playbook_overview.candidate.author` and REST `list_versions` echo it; `Playbook.created_by` stays `agent`), `test_writer_identity_default_is_agent`, `test_identity_set_inside_run_turn`, `test_delegate_toolset_reads_python_summary`, `test_v2_prompt_eleven_sections_in_order`, `test_v2_prompt_language_rules`, `test_prompt_follows_target_format`, `test_delegation_skill_names_v2_loop`, `test_read_header_warns_before_write` — all green.
- `tests/test_delegate_prompt.py`: green with the explicit pblang format (pblang variant byte-identical to the phase-00 text). `tests/test_delegation.py`: green unchanged.
- `tests/test_v2_end_to_end.py::test_agent_turn_proposes_dry_runs_runs_and_publishes` — green. The turn = `ScriptedAgent.turn()` with the `_delegation_id` ContextVar SET DIRECTLY the way `_drive_delegation` sets it (token before, reset in `finally`) — not driven through `_drive_delegation` (part a's `test_identity_set_inside_run_turn` / `test_delegated_edit_stamps_delegation_author` prove that chain; the e2e keeps one background task live: the candidate run). Asserted in order: `playbook_propose(name="pb-e2e", format="python", code=E2E_CODE, inputs_schema=…)` → `candidate_saved`, `validated True`, `candidate_version 1`, `live_version None`, keys exactly `playbook_id, name, format, status, live_version, candidate_version, runnable_via, triggers_active, publish_required, validated, warnings, next`, `next` names `playbook_run_candidate` and not `playbook_validate`, the v1 row already stamped `delegation:<uuid>`; `playbook_dry_run` → first five keys `kind, side_effects, version, version_role, run_id` = `dry_run, False, 1, candidate, None`, `status "simulated"`, `unreached_call_sites []`, zero `playbook_runs` rows, the `file_write` fake NOT called; `playbook_run_candidate(wait_seconds=30)` → envelope `candidate_test_run, True, 1, candidate, <uuid>`, `status "done"`, `result == {"written": <path>}`, exactly one run row (`is_test True`, `trigger "agent-candidate"`, `status done`), the fake called once with the real args `{path, content}`; `playbook_status(run_id)` → the same envelope + `status done`; `playbook_publish(explanation=EXPLANATION)` → `published`, `live_version 1`, `previous_live_version None`, keys exactly `playbook, status, live_version, previous_live_version, gates, evidence, note, next`, `evidence == {run_id: <the candidate run>, status: "passed"}`, gates `static_validation, test_run, probes` all ok, exactly ONE entry in `approvals.requests`; DB `(live_version, candidate_version) == (1, None)`; the LIVE row's `author == "delegation:<uuid>"` (47 chars — publish moved the pointer, minted nothing); `playbook_overview` → `candidate None`, `playbook_run_executes.version 1`, `versions[0].author == delegation:<uuid>`, `live True`; the recorded call list is exactly `propose, dry_run, run_candidate, status, publish` — no `playbook_validate`, and no `next`/`hint`/`message`/`note` text names it; the token reset (`writer_identity() == "agent"` after the turn). Canvas step (plugin/10 REST): `check(E2E_CODE)` call sites `["file_write"]`; `GET /playbooks/pb-e2e/graph?version=1` → 200, `format python`, `node_ids ⊇ {step-file_write}`; `GET /playbooks/runs/<id>` → `trace[-1] == {node: step-file_write, status: done}`, `failed_line None`, `error None`; `GET …/versions` → `{1: delegation:<uuid>}`.
- `tests/test_v2_end_to_end.py::test_rejected_card_publishes_nothing` — green: `_Approvals(decision="rejected")` → publish `status != "published"`, exactly one card, `live_version` unset (`0`/`None`), `candidate_version 1`, `playbook_overview.playbook_run_executes.version None`, `candidate.author == delegation:<uuid>`, the candidate run row stays the only row, no validate anywhere.
- `ui-src/src/playbooks/__tests__/VersionsTab.test.tsx::renders author labels` — green: rows `delegation:1234abcd-5678-4efa-9bcd-0123456789ab` / `owner` / `agent` render "delegation 1234abcd" (title = the full id) / "you" (title `owner`) / "agent"; `authorLabel('system') == 'system'`, `authorLabel('') == '—'`. `npm test` 20 files / 150 tests green; bundle rebuilt and committed (`index-CFve1-99.js` + the unchanged `index-N9IomYre.css`; `index-BmqqWkXy.js` deleted; `ui/index.html:7` names the new file).
- `tests/test_manifest_drift.py` (4) green — 26 tools, 11 tables, 3 skills, stamps agree at 0.55.0. `tests/test_loader_style_import.py` (2) green. `uvx ruff check --select F401` clean.
- Full suite: 720 collected. Run 1: `8 failed, 712 passed` — the 7 pins + `tests/test_wake_on_completion.py::test_slow_run_promises_wake_and_stamps_flag` (`row.wake_on_complete is True` false; the log shows a LEAKED run task `playbook-run-…` dying on `no such table: playbook_runs` — an in-memory-engine task from an earlier test, the Post-M3 hazard). Alone: `tests/test_wake_on_completion.py` 10 passed in 0.34 s. Run 2 (clean): `7 failed, 713 passed in 81.13 s` — red set by name = exactly the 7 entry pins (`tests/test_repro_fixplaybooks_lifecycle.py::{test_publish_success_carries_verified_readback, test_approved_then_regated_same_payload_trips_loop_guard, test_manifest_set_does_not_flip_live}`, `tests/test_repro_fixplaybooks_runtime.py::{test_interrupted_run_survives_restart_instead_of_failing, test_wait_for_approval_actually_gates, test_wait_for_event_actually_waits, test_tool_step_timeout_is_enforced}`); 0 skipped / xfail. Recorded flake, not a red (green alone, green on the re-run).
- Integration proof (Step 10: `author: delegation:<id>` on a `vaselin-*` side-load + one v1 pblang run `done`): **deferred to M6** (owner decision; M4 touches no remote machine). Risks 2 (ContextVar propagation on the real core) therefore stays proven only through the fake `run_turn` chain until M6.
- Keyhole gate verdict (Step 11): see "## Keyhole gate (dojop/02)" below.
- luna cross-check: not re-run this part (no tool, event or table change; the manifest count is unchanged). Expectation stands at luna/03's `2 failed, 42 passed` for 107 + 007.009 (`2 failed, 70 passed` with 103 + 028), reds = luna/04's two v1 pins.

## Deviations from this plan

1. Step 10 (side-load on a `vaselin-*` agent, the delegated write proof on the real core, the v1 regression-gate run) DEFERRED to M6 by owner decision — not attempted, no remote machine touched. The M4 gate line for it reads `deferred to M6`.
2. Step 9's single commit became two code commits (part a `aa17ef1` at 0.54.0, part b `0ca8b5f` at 0.55.0) plus the docs commit — the phase ran as two workflow parts; the plan's commit message was replaced by the parts' messages. Stamp bump only in part b (the bundle + ToolDef description are the reasons the plan names; part a changed a ToolDef description too but the stamp moved once).
3. The end-to-end turn sets the `_delegation_id` ContextVar directly (as `_drive_delegation` does) instead of running under `_drive_delegation` — one background task (the run) instead of two (delegation + run); part a proves the `run_turn` chain. The script adds `playbook_status` between the candidate run and publish (a fourth run-shaped result to pin the envelope on) and the plugin/10 REST canvas step; the publish assertion reads the candidate pointer from the DB and `playbook_overview` because the publish result has no `candidate_version` key (keys pinned exactly).
4. Part a's deviations stand (M4 notes "## plugin/11 part a"): python §7 FREE line drops "validate"; python §8 references §5's example; a DELEGATED edit over an `agent`-authored unpublished candidate is refused by design; `Playbook.created_by` stays `agent`; new tests use a file-backed sqlite engine per test.
5. `authorLabel` is now exported from `VersionsTab.tsx` (the test pins `system` / empty without rendering rows for them). `title` on the author span is the raw author for every row (the plan asked for it on delegation rows; the raw value is harmless and lets the test pin `owner` → "you").
6. The `playbook_agent` `[[tools]]` description in `luna-plugin.toml` was re-written by hand to the ToolDef sentence set (the drift test does not compare it — marketplace listing only).
7. `ui-src/tsconfig.tsbuildinfo` did not change in this build (phase 10 committed a regenerated one; nothing to commit here).

## Learned

- Run-shaped result key orders at 0.55.0 (dojop/02's graders key on them): `playbook_dry_run` → `kind, side_effects, version, version_role, run_id, status, dry_run, banner, steps_ran, unreached_call_sites, journal, result, error, error_type, format, tested_version, is_candidate`; `playbook_run_candidate` (done) → `kind, side_effects, version, version_role, run_id, playbook, candidate_version, status, note, step_results, result, next`; `playbook_status` (done) → `kind, side_effects, version, version_role, run_id, playbook, status, trigger, started_at, completed_at, result, steps, next`; `playbook_publish` → `playbook, status, live_version, previous_live_version, gates, evidence, note, next`; `playbook_propose` → `playbook_id, name, format, status, live_version, candidate_version, runnable_via, triggers_active, publish_required, validated, warnings, next`.
- Publish moves the live pointer onto the candidate row — the live row keeps the delegate's stamp; `playbook_overview.versions[0].author` is the way to read who wrote the live version.
- The scripted `code_run` fake works unchanged in dry mode (the dry loop writes the same entry 0 with `inputs`; the effect is stubbed instead of executed) — one script serves dry run and real run.
- Under full-suite load an in-memory-engine run task from an earlier test can outlive its test and fail on a disposed engine (`no such table`), taking an unrelated timing pin with it (`test_wake_on_completion.py::test_slow_run_promises_wake_and_stamps_flag`, `wait_seconds=0`). Third known load flake beside `test_v2_parked.py::test_wait_event_deadline_passed_during_downtime` and `test_delegation.py::test_slow_path_returns_running_then_status_polls_done`.
- `npm test` counts at exit: 20 files / 150 tests; plugin suite 720 collected.

## Revised

- `phases/12-migration-and-final-measurement/PLAN.md` :12 — dated "phase 11 landed" line: author values on migrated rows (`agent` / `delegation:<uuid>` / `owner`), the conflict guard a migration job must respect, stamp 0.55.0 → plugin/12 takes 0.56.0, the e2e harness (`_env`, `ScriptedAgent`, `_story`) and the exact result key orders, Step 10 deferred to M6.
- This file's `PLAN.md` — `Status: in progress — gate pending (dojop/02)`.
- luna-fixer M4 `PLAN.md` "## Landed in M4" — one dated plugin/11 line (both parts, commits, stamp, counts, deferral).

## Keyhole gate (dojop/02)

pending — appended by dojop/02
