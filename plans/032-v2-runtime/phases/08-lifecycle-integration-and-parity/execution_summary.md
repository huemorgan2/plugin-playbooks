# 032 — Phase 08 — execution summary

Ran: 2026-09-08, Claude (Fable 5.1) as the luna-fixer M3 executor, in three parts (Part 1 = Steps 1-6, Part 2a = Steps 7-10, Part 2b = Steps 11-12, Part 2c = Steps 13-14 + 16). plugin-playbooks `v2-runtime` at `899a8b3` (code `ee36dcc` = plugin/07; stamp 0.51.0 ×3; manifest 25 tools / 11 tables / 3 skills; suite 624 collected = 617 green + 7 red pins) → HEAD `3a1641c`. Five local commits, none pushed:
- `bf7ad01` "032 phase 08 (part 1): format per version/run, result column + key, stubs_from_run, failed_handled hoisting"
- `cded4d9` "032 phase 08 (part 1, verifier fixes): scrub step outputs/events; backfill_format fills NULL only"
- `84b8096` "032 phase 08 (part 2a): per-run card, set_autonomy note, test-run labels, parked gate text"
- `e6a9da1` "032 phase 08 (part 2b): live_version invariant, parity suite, timed_out_unknown/parked consumer rendering"
- `3a1641c` "032 phase 08 (part 2c): manifest regenerated; 0.52.0"
Phase diff `899a8b3..3a1641c`: 24 files, +3559/−137 (code + docs 15 files +1076/−116; tests 8 files +2482/−20). ` M uv.lock` left as the pre-existing unstaged change (never staged, never discarded). Stamps at exit: **0.52.0** ×3 (`pyproject.toml:3`, `plugin_playbooks/luna-plugin.toml:2`, `plugin_playbooks/__init__.py:723` — the `manifest = PluginManifest(version=...)` line moved from :655). luna `fix-playbooks` @ `7d2ac9d` (code `df6f50e` = 0.92.044) used read-only for the cross-check; no remote machine touched; nothing published, pushed or promoted.

Commands (from the plugin repo): `.venv/bin/python -m pytest -p no:cacheprovider -q -rfEsx tests` (full suite, ~80 s at exit); `uvx ruff check --select F401 plugin_playbooks tests`; `.venv/bin/python -m pytest -p no:cacheprovider -q tests/test_manifest_drift.py tests/test_loader_style_import.py` before every commit; luna cross-check from `/Users/roy/Documents/my-projects-docs/luna-fixer/luna`: `LUNA_PLUGIN_SET_DIR=<scratch>/m2/plugin-set .venv/bin/python -m pytest -p no:cacheprovider -q tests/107-fix-playbooks tests/007.009-vibe-playbook tests/103-approval-nowait tests/028-phase9.1-fixes` (the plugin-set dir symlinks `plugin_playbooks` to this working tree).

## Results

Full suite at exit (`3a1641c`): **665 collected = 658 passed + 7 failed**, 0 skipped, 0 xfail. The 7 red are exactly the entry pins, by name: `tests/test_repro_fixplaybooks_lifecycle.py::{test_publish_success_carries_verified_readback, test_approved_then_regated_same_payload_trips_loop_guard, test_manifest_set_does_not_flip_live}`, `tests/test_repro_fixplaybooks_runtime.py::{test_interrupted_run_survives_restart_instead_of_failing, test_wait_for_approval_actually_gates, test_wait_for_event_actually_waits, test_tool_step_timeout_is_enforced}`. None flipped; `test_wait_for_approval_actually_gates` still fails on its ORIGINAL assertion (`assert 'fast' not in ['fast']`, `tests/test_repro_fixplaybooks_runtime.py:137`) — it pins the v1 `wait_for_approval` STEP, untouched here. Intermediate counts: Part 1 `7 failed, 634 passed`; Part 2a `7 failed, 642 passed`; Part 2c `7 failed, 658 passed`.

Exit tests (every named file/test; all green unless stated):
- `tests/test_v2_parity.py` (18): `test_completed_payload_is_the_v1_keys_plus_result` (v2 keys = v1 keys + `result`, v1 order byte-identical, `result` last; v1 `None`, v2 `{"n": 1}`), `test_wake_text_identical_plus_result_line`, `test_fix_proposal_shape_identical`, `test_failure_digest_identical`, `test_trust_badges_from_checker_tool_list`, `test_gate_identical_for_green_and_failed_v2_candidate_runs`, `test_gate_names_a_parked_candidate_run`, `test_playbook_saved_emitted_on_publish_both_formats` (+ `TriggerBindingService._needed_events` / trigger subscription before/after publish), `test_heartbeats_identical` (`HEARTBEAT_INTERVAL` 0.05), `test_report_to_rules_identical` (table-driven on `_create_run`), `test_trigger_input_shapes_identical`, `test_edit_tickets_identical` (6 refusal texts), `test_duplicate_healing_and_mint_above_max_on_python_rows`, `test_archived_name_takeover_across_formats` (both directions), `test_delegation_card_token_flow_for_python_playbook`, `test_handled_failure_is_a_green_run_in_status_runs_digest_and_proposals`, plus two consumers added by this phase (Post-M2 checklist item): `test_timed_out_unknown_counts_as_failed_with_its_own_status`, `test_parked_is_never_finished_or_failed_in_any_consumer`. Real jail; no test drives two run tasks at once.
- `tests/test_v2_format_rows.py` (4): `test_columns_migrate_and_backfill` (columns dropped → `_ensure_columns` re-adds; old rows read `pblang`; `backfill_format` fills NULL only and returns 0 on the second call; a pblang candidate row of a python playbook is NOT rewritten on a later load), `test_mint_and_run_stamp_format`, `test_python_candidate_beside_pblang_live_runs_each_under_its_runtime`, `test_edit_may_change_format`.
- `tests/test_v2_format_tools.py::test_edit_may_change_format` — the rewrite of plugin/04's `test_edit_default_is_stored_format_and_change_refused` (rewritten, not deleted; 12 tests in the file green).
- `tests/test_v2_result.py` (5): `test_result_persisted_and_surfaced` (row, `playbook_run`, `playbook_run_candidate`, `playbook_status`, `playbook_runs`, REST `list_runs`/`get_run`), `test_v1_result_is_null_everywhere`, `test_non_json_return_fails_loud` (`TypeError`, "return value is not JSON"), `test_resolved_secret_never_stored` (row, step rows, step events and the full wake text — Result + Step outputs blocks — carry `vault:my_key`, never the resolved value), `test_subtask_returns_persisted_result`.
- `tests/test_v2_stubs_from_run.py` (6): `test_failed_run_journal_reaches_the_corrected_line` (`steps_ran["fetch#1"]["stubbed"] is True`, `send` in `unreached_call_sites`, `occurrences_used == ["fetch#1"]`), `test_recorded_failure_replays_as_the_error`, `test_pblang_run_rows_feed_a_python_candidate` (`fetch#1` and `fetch_list#1` both resolve), `test_explicit_stubs_win_and_wrong_playbook_refused`, `test_never_evidence` (no run row; gate still refuses), `test_status_hint_names_stubs_from_run`.
- `tests/test_v2_run_card.py` (6): `test_agent_must_confirm_v2_parks_at_effect_0` (immediate `parked` result with `approval_id`, `"playbook_set_autonomy" not in text`, card kind `playbook_run`, payload `{playbook, version, inputs}`, row `parked`/`parked_on`, no effect journal entries, `playbook.run.parked` emitted; approve → `done`; reject → `failed`/`Rejected`), `test_agent_must_confirm_v1_awaits_in_the_task` (no `activity.started`, heartbeat or step row before the decision), `test_wake_promised_for_parked_run`, `test_manual_only_refuses_without_autonomy_advice`, `test_set_autonomy_states_permanence` (`note` contains PERMANENT; ToolDef says "per-run owner card"), `test_candidate_test_run_cards_are_labelled` (eyebrow `test run of candidate v3`, summary prefix, payload identical to plugin/03's shape).
- `tests/test_v2_live_version_invariant.py` (2): `test_no_unlisted_live_version_writer` (scan of `plugin_playbooks/` − whitelist == `KNOWN_SIDE_DOORS`, `_propose` asserted absent, stale-whitelist check), `test_scanner_flags_a_new_writer` (fixture `p.live_version = 3` and `Playbook(live_version=2)` both reported; `pytest.raises(AssertionError)` proves the real test would be red).
- Step 10 pins unchanged and green: `tests/test_plan022_truthful_evidence.py::test_gate_puts_failed_run_in_the_failed_slot_only`, `::test_green_publish_reports_passed_evidence`. Step 9: plugin/03's approve tests unchanged and green. Step 8 proof: `grep -rn "playbook_set_autonomy(name, 'agent_may_trigger')" plugin_playbooks` → 0 hits; no `needs_approval` literal remains in `plugin_playbooks/`.
- Step 13: `tests/test_manifest_drift.py` (4) green, `tests/test_loader_style_import.py` (2) green, `uvx ruff check --select F401 plugin_playbooks tests` → "All checks passed!". Manifest: 25 tools, 11 tables, 3 skills — names/policies/risk levels unchanged; descriptions refreshed for `playbook_run`, `playbook_status`, `playbook_set_autonomy`, `playbook_runs`, `playbook_dry_run`, `playbook_edit`, `playbook_run_candidate` (the toml carries hand-written one-line summaries, not the ToolDef text — the drift test compares names/policies/risk/count only; the three the plan names plus four whose contract changed in this phase).
- `npm test`: not run — no UI change in this phase (plugin/10 rebuilds the bundle).
- Cross-repo (luna, read-only): `LUNA_PLUGIN_SET_DIR=<scratch>/m2/plugin-set` against luna `fix-playbooks` @ `7d2ac9d` (code `df6f50e`, 0.92.044, `PLAYBOOK_RESUME_KINDS` admits `playbook_run`): suites 107 + 007.009 + 103 + 028 → **63 collected: 62 passed, 1 failed** — the pre-existing `tests/007.009-vibe-playbook/test_tools.py::test_propose_accepts_valid_definition` (`'candidate_saved' == 'created'`, plugin/04 contract, luna/04 owns). `grep -rn needs_approval tests/` in luna → no hits: no luna test asserts `needs_approval`, nothing to hand to luna/03. luna working tree untouched.

## Deviations from this plan

1. Five commits instead of Step 14's ONE (`bf7ad01`, `cded4d9`, `84b8096`, `e6a9da1`, `3a1641c`): the phase ran as three verified parts under the luna-fixer M3 workflow; each part landed green and self-consistent (loader-style import test + manifest drift green before every commit).
2. Step 15 (side-load on a `vaselin-*` agent) DEFERRED to M6 by owner decision — M3 touched no remote machine. Consequence: the PostgreSQL half of `playbook_versions.format`, `playbook_runs.format` (`VARCHAR(16) NOT NULL DEFAULT 'pblang'`) and `playbook_runs.result` (`JSONB`) is unproven (SQLite proven by `test_columns_migrate_and_backfill`); they join the M6 PG-proof list beside `playbook_journal` and the `JSONB` `parked_on` ×2 (M2 item 10). The end-to-end v1 + v2 run records the plan's Step 15 asked for do not exist yet.
3. Stamp: BUMPED to **0.52.0** (Risks 11 said batch-or-bump, stated here): the `[[tools]]` descriptions, a db column set (`format` ×2, `result`) and the agent-facing contract (`playbook_run` parked/refused results, `playbook_set_autonomy` note, `stubs_from_run`, `result` key) change together — operator decision, owner may overrule (a downgrade to 0.51.0 is a three-line change before publish).
4. Step 6 (`failed_handled` exclusion) implemented VIEW-side: `_status` derives the handled step keys from the journal (`agent_tools.py::_handled_step_keys`) and never hoists the run-level error from them; the DB `PlaybookStepRun` rows keep `failed` because plugin/06/07 pin that row status. `_runs_read` failures, `fix_proposals._on_completed` and `failure_digest` key on run status (`done`), so they were already unaffected.
5. `backfill_format` fills NULL only (verifier fix, `cded4d9`): the python-playbook inheritance for pre-existing rows is a one-shot `_POST_ADD_SQL` inside the ADD COLUMN transaction of `_ensure_columns`, so a later load never rewrites a legitimately pblang candidate/run row of a python playbook.
6. Result scrub (verifier fix, `cded4d9`): besides `run()`'s return, each v2 step row's `outputs`, the `playbook.step.completed` payload and the failed-step error string are scrubbed (`SegmentLoop._scrub`, `v2/loop.py`); the JOURNAL row keeps the raw tool result (it is what resume replays against). Replacement token is the original `vault:<name>` ref (Risks 6 read as "resolved values never stored"; `vault:` literals stay).
7. pblang-target derived stubs (Step 5) also register bare `"<step_id>"`/`"<tool>"` keys, because the v1 dry-run stub lookup uses bare keys — extension of Risks 3 (operator decision, owner may overrule).
8. Step 7 as landed vs the plan text: the card is raised in `start_run_background` BEFORE any task is spawned (both formats), so `_run` returns the card id immediately; v2 spawns nothing until the decision (`ParkService._resume` → `runner.resume` → `_resume_run` drives fresh); v1 spawns the task, which awaits an in-task waiter before `activity.started`/heartbeat/first step. The row-only park variant is `ParkService.park_run(run, request_kw, in_task=<pblang>)` (no journal entry; `parked_on.gate == "run"`); rejection/expiry completes the row inside `ParkService` (single path) and the v1 waiter is released after the row is terminal. `needs_owner_card` travels as a kwarg only when True (older runner seams / test stubs).
9. `_propose`'s archived-name cross-format refusal ("changing a playbook's format is not supported yet") was lifted too (Part 2b) — the plan's Scope only named `_edit_impl`, but the exit test `test_archived_name_takeover_across_formats` requires it.
10. `agent_tools.py::_shim_playbook` was NOT collapsed onto `versioning.shim_playbook` (M2 item 13); both are whitelisted by symbol in the invariant test.
11. Step 16's "copy the verdicts into luna-fixer `phases/M3-lifecycle-parity/execution_summary.md`" and the luna/03 / dojop/02 revisions are outside this repo — left to the luna-fixer M3 gate agent (this summary is the source; the dojop/02 contract is under "Learned").
12. plugin/05's summary line 3 reads "verdict stop" while the file ends with "VERDICT: go"; Step 1 treated it as go per the M3 workflow.

## Learned

- **Completed payload key count**: `playbook.run.completed` carries **13 keys** at this phase's exit — `run_id, status, duration_ms, error, playbook_id, playbook_version, is_test, playbook_name, trigger, conversation_id, parent_run_id, wake_on_complete, result` (12 pre-existing + `result` LAST). The master's "13 keys + `result` / additive 14th key" (§3 P3) counts one too many; the suite asserts the v1 SET + `result` and the v1 order, never a literal. luna-fixer corrects master §3 P3 at the M3 gate. Any later key goes after `result` and into `COMPLETED_KEYS` in `tests/test_v2_result.py` and `tests/test_v2_parity.py`.
- Operator decisions taken for the plan's open questions — each "operator decision, owner may overrule":
  - Risks 2: `KNOWN_SIDE_DOORS = {agent_tools.py::_manifest_set}` naming plan `2026-09-06-manifest-set-live-bypass`; the scan is green and `test_manifest_set_does_not_flip_live` stays red; the invariant test insists the door is still present, so landing that P0 fix must also remove the entry.
  - Risks 7: `ctx.subtask` keeps plugin/03's refusal for a pblang child (`EffectError("subtask target '<name>' is not a v2 playbook")`); a python child returns its persisted `PlaybookRun.result` (fallback to the in-memory value when the row is not `done`).
  - Risks 8: `manual_only` refuses without a card and never names `playbook_set_autonomy` (`{status: "refused", playbook, reason}`).
  - Risks 9: the v1 row IS stamped `parked`/`parked_on` (same `playbook_status`/gate text); the wait precedes `activity.started`; a restart loses it (orphan sweep → failed; v2 park survives via `ParkService.reconcile`, which fails a pblang run-gate row `RunLost`).
  - Risks 10: test-run label = `presentation["eyebrow"]` + `summary` prefix on `playbook_effect` and `playbook_run` cards of `is_test` runs; `payload` untouched.
  - Risks 11: bump to 0.52.0 (Deviations 3).
  - Phase 07 Risks 10 / M2 item 4 — **owner question, option (b) taken**: a child run that parks inside `ctx.subtask` still fails the parent's effect (`_effect_subtask` accepts only `done`); the child sits `parked` and completes on its own. Option (a) (the parent waits through the child's park, or parks itself on the child) is not implemented. Owner to decide whether (a) is wanted before plugin/12 retires pblang.
  - `timed_out_unknown`: counts as a FAILURE for `failure_digest`/`fix_proposals` (finished + failed) with `error_type: OutcomeUnknown` surfaced (`models.FAILED_RUN_STATUSES = ("failed", "timed_out_unknown")`; the digest line is prefixed `OutcomeUnknown: `, the proposal card carries `error_type`/`status`, the wake adds the "Outcome unknown … check the target system" sentence); `_status`/`_runs_read` render it with its own status string plus an outcome-unknown hint; `publish.latest_run_evidence` puts it in the FAILED slot (never evidence). `parked` is never finished/failed anywhere.
  - Phase 06 Risks 4 (parent `subtask` row vs child result on restart): NOT trivial once `ctx.subtask` reads the persisted result — the parent's step row still holds what the loop recorded at effect time; recorded open (M6 / plugin/12).
- Consumers added to the master §1 item 13 list by this phase: `timed_out_unknown` and `parked` rendering in wake / fix proposals / failure digest / `_status` / `_runs_read` / `latest_run_evidence` (two parity tests). No consumer outside the list was found otherwise; the delegation card body carries an extra `waiting_for_approval` key (pinned as a superset).
- Parity findings pinned as-is: v2 step ids carry `#n` (wake/proposal parity asserted after normalizing the suffix, uuids, durations); a failed v2 run's error is the one-liner (v1: abort text) — compared as "Error: <error>" shape + honesty line; v2 unhandled tool `error_type` is `ToolError` (not `EffectError`); trigger intake is identical (row `inputs`, `InputTypeError` text) but a v1 template `n=inputs.n` renders scalars to text ("7") while python passes the typed 7 — pre-existing v1 semantics, owner question whether parity should extend to template rendering; the trigger rejection path emits `playbook.run.started` for the recorded failed row.
- Shapes later phases read (see the phase 09-12 revisions): `parked_on` for the per-run card `{"kind": "approval", "approval_id": <str UUID>, "since", "due_at", "gate": "run"}`; `_run` parked result `{run_id, playbook, status: "parked", approval_id, message}`; card kinds `playbook_change` / `playbook_effect` / `playbook_run` (payload `{playbook, version, inputs}`); `stubs_source` `{run_id, version, format, status, occurrences_used, occurrences_unmatched}` (only when accepted); journal read API `runner._v2.journal.read(str(run_id)) -> list[dict]`; format-changing edits: candidate row carries the new format, `Playbook.format` flips on publish/rollback, READ header/write result carry `format` + `live_format`.
- dojoP contract (for dojop/02's keyhole graders): `playbook_run` on `agent_must_confirm` → `status: "parked"` + `approval_id` + the "waiting on owner card … nothing to poll" message (never `needs_approval`, never `playbook_set_autonomy`); `result` in run results; the failed-run `playbook_status` hint names `stubs_from_run`; `playbook_set_autonomy` results carry the permanence `note`.
- Anchors at `3a1641c` (informative): `models.py` `PlaybookVersion.format`, `PlaybookRun.format`, `PlaybookRun.result`, `FAILED_RUN_STATUSES`; `__init__.py` `_COLUMN_MIGRATIONS` (3 new rows), `backfill_format`, manifest `version="0.52.0"` :723; `versioning.py` `mint_version(..., format=)`, `shim_playbook`; `runner.py` `_create_run` (`format=fmt`), `_drive_run(..., await_card=)`, `start_run_background(..., needs_owner_card=False)`, `_raise_run_card`, `_complete_run(..., result=)`; `v2/loop.py` `scrub_secrets`, `_persisted_result`, `_dry_failure`, `_effect_approve` → `label_test_run`; `v2/park.py` `RUN_GATE`, `label_test_run`, `park_run`, `wait_run`, `_resume_run_gate`; `v2/dry.py` `raise_marker`; `agent_tools.py` `_stubs_from_recorded_run`, `_handled_step_keys`, `_run` (manual_only / parked branches), `_set_autonomy` note, `_dry_run(..., stubs_from_run=)`; `publish.py` `_parked_run`, `test_run_gate` parked branch; `wake.py` `_failure_lines`; `docs/v2.md` §10 `stubs_from_run` paragraph.
- Open items carried to M6 / later phases: PG proof of `format` ×2 + `result` (Deviations 2); no `result` size cap (an owner-set cap is a one-liner); phase 06 Risks 4 (above); the `on_server_ready`-not-on-upgrade note (M2 item 6) — this phase did not touch lifecycle wiring; journal retention (M2 item 5) untouched; `resume(from_park=True)` `_timeout` loss (M2 item 8) not hit by the per-run card (no `_timeout`).

## Revised

- `phases/09-provenance-and-overview/PLAN.md` (Depends on :5, Cross-repo plugin/08 :101, Risks 1 :106): dated "phase 08" lines with the landed `result`/`format` surfaces, the 13-key payload, `parked_on` incl. `gate: "run"` and the str-UUID `approval_id`, the three card kinds, the parked/refused `_run` shapes, `timed_out_unknown`/`parked` rendering, stamp 0.52.0 (09 bumps to 0.53.0 for the 26th tool), and the `_status` hint ordering (overview pointer after the `stubs_from_run` sentence).
- `phases/10-canvas/PLAN.md` (Depends on :9, Goal :15): per-run/per-version `format` as the graph/dispatch truth, `result` on `get_run`/`list_runs`, per-occurrence step rows `step_id == "<id>#<n>"` (split on `#` before matching `step-<id>`), overlay status from the journal entry (`failed_handled` rows stay `failed` in the DB).
- `phases/11-delegation-v2-and-keyhole-gate/PLAN.md` (Depends on :5, "Delegate prompt v2 variant" :76): the per-run card in the delegation prompt (parked result → report and end the turn; never `playbook_set_autonomy`; parked-candidate publish refusal → wait, never a second candidate run; `result`; the `stubs_from_run` fix loop), the parked status text, the labelled test-run cards, the parity/invariant pins the delegation side must keep green.
- `phases/12-migration-and-final-measurement/PLAN.md` (Depends on :9, Scope 1 :23, Not in this phase :38): `stubs_from_run` on pblang runs as landed (keys incl. bare keys, `_raise` stubs), the frozen `stubs_source` shape and refusal shape, `_stubs_from_recorded_run` + journal read API, the format-changing edit contract (candidate row carries the format; `Playbook.format` flips on publish; `format`/`live_format`/`next`; archived-name takeover both ways; `compare=true` reads `PlaybookRun.format`).
- This file's `PLAN.md` status line → `done`.
- NOT edited (outside this repo): luna-fixer `phases/M3-lifecycle-parity/execution_summary.md`, luna/03, dojop/02, master §3 P3 key count — handed to the M3 gate agent through this summary.
