# 032 — Phase 04: format column and tool wiring, propose = candidate, loud intake, edit-error payload, error surfacing, interim UI — execution summary

Status: done (independent verifier green, 0 fix rounds). Stamps 0.47.0 → 0.48.0.

## Ran
- Date: 2026-09-08. Repo `luna-plugins/plugins/plugin-playbooks`, branch `v2-runtime`. HEAD before `ac51433` (plugin/03 summary), HEAD after `17bbd47` — one commit "032 phase 04: format column, propose = candidate, loud intake, edit payload, error surfacing, interim UI", not pushed (`git branch -r --contains HEAD` empty; origin has only `main`). luna and dojoP untouched. Steps 1-13 of the phase file executed; step 14 is this document.
- Commit `17bbd47`: 38 files, +2457/-236. Production: `plugin_playbooks/models.py` (+9), `__init__.py` (42), `versioning.py` (16), `runner.py` (53), `routes.py` (21), `triggers.py` (+26), `trigger_bindings.py` (+4), `probes.py` (11), `fix_proposals.py` (35), `agent_tools.py` (845), `luna-plugin.toml` (20), `pyproject.toml` (2). UI: `ui-src/src/playbooks/V2View.tsx` (new, 106), `types.ts` (6), `PlaybookEditor.tsx` (+4), `VersionsTab.tsx` (+13), `__tests__/V2View.test.tsx` (new, 147), `tsconfig.tsbuildinfo`; bundle pair `plugin_playbooks/ui/assets/index-C79IwJhG.js` + `index-hAWF1dL_.css` committed, `ui/index.html:7-8` updated, stale pair `index-DxyKmHkO.js` / `index-C5Bp-kfQ.css` deleted (vite `emptyOutDir`). Tests: new `tests/v2harness.py` (85), `test_v2_lifecycle.py` (233), `test_v2_intake.py` (220), `test_v2_edit_payload.py` (160), `test_v2_format_tools.py` (227), `test_v2_error_surfacing.py` (198); existing tests touched: `test_archive_name_squat.py`, `test_build_operate.py`, `test_candidate_flow.py`, `test_code_tools.py`, `test_explainable_publish.py`, `test_plan022_truthful_evidence.py`, `test_probes.py`, `test_repro_fixplaybooks_lifecycle.py`, `test_v2_effects.py`, `test_v2_loop.py` (detail under Results). Staged by explicit path; ` M uv.lock` left as found, not staged; no `plans/` file, no `.env` in the commit. Secrets grep over the non-bundle diff (`lsk_`, `api_key`, `secret`, `password`, private keys, `postgres://`, `AKIA`) → 0 hits.
- Commands (plugin venv `.venv/bin/python -m pytest -q -p no:cacheprovider`, no `uv sync`):
  - `tests/test_v2_lifecycle.py` → 9 passed; `tests/test_v2_intake.py` → 6 passed; `tests/test_v2_edit_payload.py` → 5 passed; `tests/test_v2_format_tools.py` → 12 passed; `tests/test_v2_error_surfacing.py` → 4 passed — 36 new cases, all green.
  - `tests/test_v2_checker.py` (plugin/01, untouched) → 96 passed; `tests/test_v2_loop.py` → 27 passed; `tests/test_manifest_drift.py` → 4 passed (tables 10, tools 25, stamps 0.48.0 at `pyproject.toml:3`, `plugin_playbooks/luna-plugin.toml:2`, `plugin_playbooks/__init__.py:647`).
  - Full suite `tests -q` → 7 failed, 554 passed in 36.22 s; 561 collected = 525 at `ac51433` (518 green + 7 red) + 36 new.
  - `cd ui-src && npm ci && npm test` → 18 files / 130 tests passed; `npx vitest run src/playbooks/__tests__/V2View.test.tsx` → 4 passed; `npm run build` → the committed pair (the verifier's rebuild reproduced it byte-for-byte, `git status` unchanged).
  - `uvx ruff check --select F401 plugin_playbooks tests` → "All checks passed!" (verifier; the executor used `.venv/bin/python -m compileall -q plugin_playbooks` because ruff is not in the venv — nothing installed).
- Independent verifier: re-ran every suite above at `17bbd47` with identical totals; 0 fix rounds.

## Results
Exit tests (all green):
- `tests/test_v2_lifecycle.py`: `test_propose_returns_candidate_saved_exact_shape[pblang|python]` (result ⊇ the six master keys with `live_version None`, `candidate_version 1`, `runnable_via "playbook_run_candidate"`, `triggers_active False`, `publish_required True`; `format` echoed; `validated is True`; row `(version 1, live_version 0, candidate_version 1)`; one `PlaybookVersion` (v1, `agent`); `versioning.live_version_of` and the routes twin `None`); `test_run_refuses_naming_the_candidate` (error names "candidate v1" and "playbook_run_candidate", `publish_required True`, no run row — this also exercises the agent_tools twin, a `build_tools` closure at `agent_tools.py:1289`); `test_recreate_no_longer_promotes` (candidate v2, live stays 1, row `code` still v1; never-published archived row → live still None); `test_publish_v1_of_new_playbook_goes_through_gate_and_card` (test_run gate text without a green run; one card; `live_version 1`, `candidate_version None`, `previous_live_version None`); `test_triggers_inactive_until_publish` (`_unsubs == {}` and no needed event before publish, `x` in both after); `test_backfill_does_not_promote_candidate_only_rows` ((0,1) stays 0; (0,None,v3) → 3); `test_digest_and_fix_proposals_skip_live_none`; `test_rollback_and_ack_refuse_without_live` (both errors contain "no live version").
- `tests/test_v2_intake.py`: `test_number_string_reaches_run_as_4_on_chat_path` and `_on_trigger_path` (`"4"` → `4`, not `str`; `run.inputs["n"] == 4`); `test_uncoercible_input_fails_at_intake_on_chat_path` (`status "rejected"`, `error == "input 'n' expects number, got 'abc'"`, `input "n"`, `expected "number"`, no run row, `echo` never called); `_on_trigger_path` (one run row `failed`, `error_type "InputTypeError"`, `echo` never called, `_in_flight` empty); `test_v1_playbook_gets_the_same_loud_intake`; `test_coerce_inputs_raises_typed_error` (`.input "count"`, `.expected "integer"`). `tests/test_plan022_truthful_evidence.py::test_coerce_inputs_rejects_uncoercible_values` (renamed from `..._leaves_uncoercible_values_alone`, now `:420`) asserts the raise; `::test_coerce_inputs_casts_scalars_to_declared_types` (`:401`) unchanged.
- `tests/test_v2_edit_payload.py`: `test_checker_error_on_edit_keeps_ticket` (exactly the 9 keys `stage, saved, format, errors, warnings, ticket, ticket_still_valid, expires_in_seconds, retry`; `0 < expires_in_seconds <= 900`; `retry` verbatim; `errors[0]["code"] == "v2-use-ctx-log"` with `example_fix`; no `language_reference`; row `version 1`; ticket `used_at None`); `test_same_ticket_reusable_after_checker_error` (second write → `candidate_saved`, `candidate_version 2`, `validated True`; third write → used-ticket refusal); `test_compile_error_on_pblang_edit_keeps_ticket_with_cheatsheet` (the 9 keys + `language_reference == LANGUAGE_CHEATSHEET` + `error` — see Deviations 1); `test_green_write_says_validated` (`validated True`, "do not call playbook_validate" in `next.lower()`, `format` echoed, `live_version None`, `next` names publish); `test_ticket_text_no_longer_says_single_use` (READ `instructions` and the `playbook_edit` ToolDef description).
- `tests/test_v2_format_tools.py`: `test_format_column_migrates` (column added by `_ensure_columns`, existing row reads `pblang`, second call adds nothing); `test_propose_precedence_table` (6 rows: python saved / pblang saved / `v2-format-mismatch` ×2 with no row / `v2-format-unknown` no row / prose → python default `v2-entry-point` no row; `format` on every result); `test_edit_default_is_stored_format_and_change_refused` (`saved False`, `format "pblang"`, `ticket_still_valid True`, row unchanged); `test_validate_echoes_format` (`ok True`/`format "python"`/`saved False`; stored-format default; `definition_yaml` + python → "pblang only"); `test_riders_absent_on_python_paths`; `test_tool_descriptions_are_format_aware` (three ToolDefs mention "python" and "pblang", `format.enum == ["pblang", "python"]`); `test_python_definition_summary_serves_readers` (`definition` has `name`, `format`, `triggers`, `inputs`, `tools`, `call_sites`; `collect_tools` == sorted tools + `code_run`; `_derive_code` returns the source; `playbook_get_definition(format="json")` returns the summary; `playbook_dry_run` → the "not available yet" refusal).
- `tests/test_v2_error_surfacing.py`: `test_compute_exception_visible_in_playbook_run_result` (`error.startswith("line 7: <src> → KeyError: 'items' after effect fetch#1")`, "Do NOT fabricate" in `error`, `error_type "KeyError"`, `failed_at` set; same line via `playbook_run_candidate`); `test_compute_exception_visible_in_status_runs_and_digest` (`playbook_status` `error`/`error_type`/non-empty `traceback`; `playbook_runs` failed entry `error`; `render_failure_section` text contains the one-liner); `test_failure_signature_dedupes_same_line` (one `PlaybookFixProposal` for two same-line runs, `signature == failure_signature(name, "line 7", "KeyError: 'items'")`; line 9 → a second row); `test_v1_failed_result_text_unchanged` (`error` = the v1 sentence, `error_detail` present).
- UI `ui-src/src/playbooks/__tests__/V2View.test.tsx` (4): header "python playbook — the graph view arrives with the canvas phase"; `getByTestId('v2-code')` contains the source and not `call_sites` (never `sourceFor`'s JSON fallback); zero `.react-flow__node`; three `v2-step` rows in order with id/kind/status and the failed row's error; `VersionsTab` with `format: 'pblang'` still renders `VersionCanvas`. `VersionsTab.test.tsx` / `VersionCanvas.test.tsx` untouched (git diff empty).
- Existing suite: 7 red before, 7 red after, no flip — lifecycle ×3 (`test_publish_success_carries_verified_readback` now asserting at `:137`, `test_approved_then_regated_same_payload_trips_loop_guard` `:181`, `test_manifest_set_does_not_flip_live` `:221` — shifted from `:134/:168/:203` by the setup-only fixture insertions, +20/-1 lines; each fails on its ORIGINAL assertion message: "publish result carries no read-back verification…", "re-issue after an approval minted a SECOND card…", "manifest_set moved live_version to 3…"), runtime ×4 (`test_interrupted_run_survives_restart_instead_of_failing` `:118`, `test_wait_for_approval_actually_gates` `:137`, `test_wait_for_event_actually_waits` `:159`, `test_tool_step_timeout_is_enforced` `:180`; file untouched).
- Step 10 / Risks 15 — every existing test touched, with the kind of change: assertion-level (subject = the removed propose-goes-live behaviour): `tests/test_candidate_flow.py` `test_propose_goes_live_directly` → `test_propose_saves_a_candidate_not_live` (`:143`), `tests/test_archive_name_squat.py::test_propose_over_archived_name_takes_over_the_row` (`:101-109` → candidate shape), `tests/test_code_tools.py` (`"created"` → `"candidate_saved"`). Setup-only (publish v1 through the gate after propose, or seed `live_version` where the stub cannot approve; clear the setup card/events): `test_candidate_flow.py` (remaining flows), `test_build_operate.py`, `test_explainable_publish.py`, `test_plan022_truthful_evidence.py` (besides the rename), `test_probes.py`, `test_v2_effects.py`, `test_v2_loop.py` (rows get `format="python"`), `test_repro_fixplaybooks_lifecycle.py` (`_green_candidate` and `test_manifest_set_does_not_flip_live` publish v1 through the gate; the loop-guard test seeds `live_version=1, candidate_version=None` — Risks 4). No test deleted beyond the two renames; no skip/xfail added.
- Step 12: `luna-plugin.toml` `[[tools]]` descriptions rewritten format-aware for `playbook_propose`, `playbook_run`, `playbook_status`, `playbook_get_definition`, `playbook_runs`, `playbook_validate`, `playbook_dry_run`, `playbook_edit`, `playbook_run_candidate`; names, policies, `risk_level`, count 25 unchanged.
- Load flake: `test_delegation` flaked once mid-run; passed in both final full-suite runs (executor) and the verifier's run. No change made.

## Deviations from this plan
1. pblang compile-error rejected edit payload (`test_compile_error_on_pblang_edit_keeps_ticket_with_cheatsheet`) carries an extra `error` key ("The playbook code does not compile — fix these and retry.", `agent_tools.py:106`, kept at `:2275`) alongside the phase's 9 keys + `language_reference`. `tests/test_code_tools.py::test_snippet_edit_that_breaks_compile_is_rejected` (not named in the plan) keys on `out["error"]`; dropping the key would have weakened that test. Additive; the plan's key list omitted the pre-032 compat sentence (plan defect, not execution).
2. `_shim_for` (named for routes.py) does not exist; routes.py has `_live_version_of` (`:423`) delegating to `versioning.live_version_of` (`versioning.py:17`) and no shim builder. The only shim is `agent_tools.py::_shim_playbook` (`:1331`, a `build_tools` closure), which now carries `format=` (`:1347` passes `live_version=row.version` on the transient shim — the candidate number, as before).
3. `ruff` not in `.venv`; `python -m compileall` was the executor's lint proof, the verifier ran `uvx ruff check --select F401` (isolated) clean.
4. v1 tool-step args are still Jinja-rendered strings (`"4.0"` reaches the v1 tool); the intake test asserts the stored `run.inputs` are coerced numbers — v1's rendering contract is unchanged, as the phase scopes it.
5. Harness placed in a shared `tests/v2harness.py` (`env(*, script=None, decision="approved", **fake_tools) -> Env`; `PY_CODE = test_v2_checker.EXAMPLE`; `CODE` = the pblang greeter; re-exports `ScriptedCodeRun`, `_effect`, `_error`, `_Approvals`, `_Ctx`) rather than inline per test file.
6. `StepRunDetail.kind` widened to `StepKind | (string & {})` (`types.ts:168`) — required by `tsc -b` for effect kinds (`tool`, `log`) in V2View rows; `StepKind` itself is not widened.
7. Line numbers cited in the phase file (at 8c31a60) had drifted; every edit located by symbol/text.
8. `_AUTHORING_SKILL_BODY`: the WRITE sentence now reads "`playbook_propose(name, code=...)` to create — it saves a CANDIDATE (validated, not live): nothing runs via triggers or `playbook_run` until `playbook_publish`" (`__init__.py:230-233`); `delegation.py:261` ("Create with playbook_propose (pass manifest=)") never claimed live and is unchanged — plugin/11 Risks 7's "phase 04 rewrites the propose sentence (:266)" did not happen.
9. The propose result carries NO `next` key (`agent_tools.py:564-576`: `playbook_id, name, format, status, live_version, candidate_version, runnable_via, triggers_active, publish_required, validated, warnings`); the edit green-write `next` (`:2354-2361`) is where "Do not call playbook_validate — this write was validated." and "No live version yet — triggers and playbook_run stay off …" live. plugin/09's "the plugin/04 `candidate_saved` result's `next`" must add the key, not extend it.
10. `uv.lock` pre-existing modification left in place, uncommitted.

## Learned
- Anchors at `17bbd47` — `agent_tools.py`: `_compile_code` :100 (`error` sentence :106), `_derive_code` :114, `_TICKET_TTL_SECONDS` :252, `_check_ticket` :286, `_propose` :361 (mint `author="agent"` :539, result :564-576), `playbook_propose` ToolDef :580, `_run` :690 (candidate-only refusal :715, `InputTypeError` catch :747), `playbook_run` ToolDef :843, `_status` :880, `_ack_failures` :1224, `_live_version_of` closure :1289, `_ensure_live_row` :1299, `_apply_version_to_live` :1314 (writer :1329), `_shim_playbook` :1331, `_playbook_get_definition` :1352, `_versions` :1403, `_version_read` :1472, `_runs_read` :1626, `_validate` :1724, `playbook_validate` ToolDef :1833, `_resolve_target` :1885, `_dry_run` :1926 (python refusal inside), `playbook_dry_run` ToolDef :1980, `_edit_impl` :2027 (rejected-write payload :2153/:2233, mint `author="agent"` :2333, green `next` :2354-2361, `validated` :2376), `_EDIT_PAYLOAD_PROPS` :2400, `playbook_edit` ToolDef :2419, `_manifest_set` :2456 (mint `author="agent"` :2473, live writer :2476), `_do_publish` :2699, `playbook_publish` ToolDef :2966, `_rollback` :3023, `_run_candidate` :3101 (`InputTypeError` catch :3142), `_preflight` :3235.
- `runner.py`: `InputTypeError(ValueError)` :239 (`.input`, `.expected`, `.got`), `_coerce_inputs` :251 (raises), `start_run` :344, `start_run_background` :366, `_drive_run` :531 (format dispatch :557-560, above `PlaybookDef.model_validate`), `dry_run` :670 (still v1-only; no `InputTypeError` handling on the dry path — plugin/05), `_complete_run` :1503. Format accessor: `format` column first, `sniff_format(code)` fallback for rows/shims without the attribute (:301-309).
- `routes.py`: `_live_version_of` :423, `_ensure_live_row` :435 (returns `None` for live-None), `list_playbooks` :583 (`format` on entries), `get_playbook` :630 (`"format": p.format`), `start_run` :887, `get_run` :934, `list_versions` :979, `get_version` :1043 (no `format` in its payload — plugin/10 Scope 2 still applies), `_apply_row_to_live` :1100 (writer :1115). Owner REST paths still write live: `live_version=1` at :702 and :1492, `p.live_version = p.version` at :747 and :1384.
- `versioning.py`: `live_version_of` :17 (`None` iff `live_version == 0 and candidate_version is not None`), `get_version_row` :43, `ensure_live_row` :61 (`-> PlaybookVersion | None`), `mint_version` :83 (`p.version = max(p.version, max_stored) + 1` :106; no `format` parameter — plugin/08 adds it).
- `__init__.py`: `_COLUMN_MIGRATIONS` :19 (the `format` row :42), `_ensure_columns` :52, `backfill_code` :134 (skips python), `backfill_live_version` :181 (`.where(live_version == 0, candidate_version.is_(None))` :198-199), `failure_digest` :462 (filter `(live_version != 0) | (candidate_version.is_(None))` :503), version stamp :647, `skills=[` :662 (v1 `SkillDef` :663, delegation :694), `playbook.saved` subscribe :882, `AUTHORING_TOOLS` :897, `_register_tool` :924, `_AUTHORING_SKILL_BODY` :211, `_DELEGATION_SKILL_BODY` :586. `delegation.py`: `_referenced_tools` :141, `delegate_toolset` :165, `_PROMPT_TAIL` :195, `_delegate_prompt` :203, `_drive_delegation` :583, `_playbook_agent` :713.
- `fix_proposals.py`: `_v2_signature_parts(one_liner) -> (line, error head)` :38, `failure_signature` :50, `_file_proposal_inner` :109 (skips live-None :129). `probes.collect_tools` :29 (python summary tools + `code_run`), `collect_subtasks` :61. `triggers.py:59` and `trigger_bindings.py:93` (`_needed_events` :85) skip live-None. `v2/loop.py`: `V2RunError` :51, `one_liner` :136.
- live_version writers remaining in `plugin_playbooks/` at `17bbd47` (for plugin/08's whitelist): `__init__.py:199` (backfill), `agent_tools.py:1329` (`_apply_version_to_live`), `:1347` (`_shim_playbook` kwarg on the transient shim), `:2476` (`_manifest_set` — P0 side door), `routes.py:702`, `:747`, `:1115` (`_apply_row_to_live`), `:1384`, `:1492`. `_propose` passes none.
- UI: `types.ts` `PlaybookSummary.format?: 'pblang' | 'python'` :121, `StepRunDetail.kind: StepKind | (string & {})` :168; `PlaybookEditor.tsx` `meta.format` :53/:118 → `VersionsTab` prop :359; `VersionsTab.tsx` prop `format?` :216/:227, branch `view === 'canvas' && format === 'python'` → `V2View` :520; `V2View.tsx` testids `v2-code`, `v2-step`. `api.ts` unchanged (`get` already returns `PlaybookSummary & {...}`). Bundle pair `index-C79IwJhG.js` / `index-hAWF1dL_.css` (`ui/index.html:7-8`).
- Tool contracts for plugin/05 and plugin/11: `playbook_propose(name, code, format?, inputs_schema?, triggers?, manifest?, agent_autonomy?)` → the 11-key result above (no `next`); `playbook_edit` write rejected → `{stage, saved:false, format, errors, warnings, ticket, ticket_still_valid:true, expires_in_seconds, retry}` (+ `language_reference` and `error` on pblang compile errors); format change on edit → `{stage, saved:false, format:<stored>, error, ticket, ticket_still_valid:true}`; green write → `validated: true`, `format`, `live_version` (null for candidate-only), `next` starting "Do not call playbook_validate — this write was validated."; `playbook_validate(code, name?, format?, definition_yaml?)` echoes `format`; `playbook_dry_run` on python → `{"ok": false, "error": "dry run for python playbooks is not available yet — use playbook_run_candidate"}`; `playbook_run` on candidate-only → `{error, candidate_version, runnable_via, publish_required}`; intake rejection → `{"status": "rejected", "error", "input", "expected"}`.
- Suite baseline for plugin/05: 561 collected = 554 green + 7 red at `17bbd47`; stamps 0.48.0; tools 25; tables 10; `npm test` 130.
- Ticket TTL is 15 min (`_TICKET_TTL_SECONDS = 15 * 60`, :252); the exit test's `<= 900` bound holds.

## Revised
- This file's phase `PLAN.md`: `Status: done`.
- `phases/05-dry-run-skill-and-go-no-go/PLAN.md`: Scope 2(d) (`_coerce_inputs` now `runner.py:251`, `InputTypeError` :239, already loud); Scope 3 (`_dry_run` :1926, `_resolve_target` :1885, `runner.dry_run` :670, `playbook_dry_run` ToolDef :1980); Scope 5 (`skills=[` :662, stamp :647); Scope 8 and Risks 10 (0.48.0 confirmed → 0.49.0); Step 8 (baseline 561 = 554 + 7); Exit-test harness note (`tests/v2harness.py`); Cross-repo plugin/04 line (rider anchors, propose result has no `next`).
- `phases/10-canvas/PLAN.md`: Depends line (interim `V2View.tsx` + `V2View.test.tsx`, `StepRunDetail.kind` widened); Scope 2 (`_live_version_of` :423, `get_version` :1043 without `format`, `get_run` :934, `list_versions` :979); Scope 4 (`StepRunDetail.kind` note); Scope 9 (VersionsTab branch :520 and prop :227); Scope 10 (bundle pair `index-C79IwJhG.js`/`index-hAWF1dL_.css`, stamp :647); Exit test `get_run` key set (:934); Cross-repo plugin/04 line (`get_playbook` :630, list :583).
- `phases/11-delegation-v2-and-keyhole-gate/PLAN.md`: Author stamping (mint sites at `agent_tools.py:539`, `:2333`, `:2473`); Candidate-conflict guard (`_EDIT_PAYLOAD_PROPS` :2400, `playbook_edit` ToolDef :2419, `_manifest_set` :2456/:2476); Step 8 (propose result has no `next`; `_run_candidate` :3101; harness `tests/v2harness.py`); Cross-repo plugin/04 (edit-error shape incl. the extra `error` key on pblang compile errors; `AUTHORING_TOOLS` :897); Risks 7 (phase 04 did not rewrite `delegation.py:261`); stamp :647.
- `phases/08-lifecycle-integration-and-parity/PLAN.md`: Stamping bullet (`_shim_for` does not exist — `_shim_playbook` :1331 only; `_apply_version_to_live` :1314, `_apply_row_to_live` routes.py:1100); writer whitelist (the writer list at `17bbd47` above; `_propose` :300/:323 gone); `mint_version` :83.
- Repo `PLAN.md`: no correction requested by this phase file; none made. luna cross-repo 007.009 suite and the luna-fixer M1 summary copy were not run/written in this part (orchestrator's later step).
