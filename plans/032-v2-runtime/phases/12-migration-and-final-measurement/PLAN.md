# 032 — Phase 12: Migration of live pblang playbooks and the final measurement

Status: in progress (2026-09-08: build part landed at `5714e5d`, stamp 0.56.0 — migrate.py, `compare` option, `tests/test_v2_migration.py`; Steps 4-6 deferred to M6 on `vaselin-error-log-tracker` by operator decision M5-1, owner may overrule; `done` comes with the dojop/03 block)

Master: /Users/roy/Documents/my-projects-docs/luna-fixer/plans/2026-09-06-fix-playbooks/PLAN.md — §3 P5 (no transpiler; count at start; per playbook rewrite → dry run with `stubs_from_run=<last green live run>` "reaches the same effects with the same args" → real candidate run → owner publishes; v1 runner + cheat sheet retired only when zero pblang playbooks remain, separate owner decision), §3 Success criteria (the eight criteria and the stop rule), §2 Dry run (`stubs_from_run` semantics, per-occurrence `id#n` keys, `unreached_call_sites`, never satisfies the test_run gate), §2 Properties and §1 item 4 (vault refs stay raw in rows and journals; `runner.py:759-812`), §4 Rollout ("plugin-playbooks" bullet: branch build side-loaded on `vaselin-*` only, v1 playbooks keep working at every phase; "Ship": nothing published, pushed or promoted before the owner approves the final state); master phase M5 (luna-fixer `phases/M5-migration-measurement/PLAN.md` steps 1-4, owner touchpoints, risks 1-3).

Repo / branch: plugin-playbooks v2-runtime (code citations verified at 8c31a60; HEAD is 5306f7f at review — the plan-writing commit, `git diff 8c31a60 5306f7f --stat -- plugin_playbooks tests` empty, so every `path:line` below holds at HEAD; origin/main 749f126, version 0.46.0 in `pyproject.toml:3`, `plugin_playbooks/luna-plugin.toml:2`, `plugin_playbooks/__init__.py:624`). Read-only references: dojoP main @ 316f007 (`plans/0002-fix-playbooks-bench/PLAN.md` section "03 — Final measurement" and `phases/03-final-measurement/PLAN.md`, which exists at that commit — it was empty at f51915d), luna fix-playbooks @ f05bdf2 (`plugin-set.toml:62-65` pins plugin-playbooks 0.39.0). Commits stay local on `v2-runtime`; nothing is pushed or published.

Depends on: plugin/08 (`stubs_from_run`, parity, `tests/test_v2_parity.py`), plugin/11 (the M4 build; dojop/02 `VERDICT: go` recorded in its summary), dojop/03 (runs against this phase's build; its per-criterion table closes this phase — see "Steps" 8 for the ordering). 2026-09-08 (phase 08 landed, stamp **0.52.0**): `stubs_from_run` works on pblang runs as planned — `PlaybookStepRun` rows in `started_at` order register `"<step_id>#<n>"` and `"<tool>#<n>"` (tool rows → `outputs["result"]`, else `outputs`), and when the TARGET is pblang the bare `"<step_id>"`/`"<tool>"` keys are added too; failed rows become `_raise` stubs (`{"_raise": {"type", "message"}}`) that raise the recorded error in the dry run. `stubs_source` shape (present only when `stubs_from_run` was accepted): `{"run_id": str, "version": int, "format": "python"|"pblang", "status": <run status>, "occurrences_used": [keys], "occurrences_unmatched": [keys]}`; refusals (`run <id> not found`, `run <id> belongs to playbook '<other>'`) return `{"error", "dry_run": True}` with no `steps_ran`; explicit `stubs=` win key by key; `occurrences_used` for a pblang source matches by step_id or `output.tool` of the trace. Format-changing edit contract: `playbook_edit`/`playbook_propose` no longer refuse a format change — the candidate row is minted with the resolved `format`, `Playbook.format` stays the live format until `playbook_publish`/`playbook_rollback` copies `row.format`; the READ header and write result carry `format` (candidate) and `live_format`, and `next` says "candidate v<N> is python; live v<M> stays pblang until publish"; the archived-name cross-format takeover is admitted both ways (`tests/test_v2_parity.py::test_archived_name_takeover_across_formats`). Each run row carries its own `format`, so a python candidate run and a pblang live run of the same playbook coexist in `playbook_runs`.
2026-09-08 (phase 09 landed, stamp **0.53.0**, 26 tools): every run-shaped tool result opens with the provenance envelope `kind, side_effects, version, version_role, run_id` (`plugin_playbooks/provenance.py`; `kind` derived from the ROW — `is_test or trigger == "agent-candidate"` → `candidate_test_run`, else `real_run`; never from the playbook's current pointers, so a historical candidate run stays `candidate_test_run` after promotion); `playbook_dry_run` reports `status: simulated` for v1 too (mapped at the tool boundary only — `runner.dry_run` still returns `done`, pinned by `tests/test_dry_stub_diagnostic.py` / `tests/test_plan004_code_and_functions.py`; `tests/test_no_spec_feature.py::test_dry_run_tool_scripts_stubs_by_step_id_and_tool_name` now pins `simulated`). Final-measurement graders that key on run results must read `kind`/`version` from the envelope, and `playbook_overview(name)` (`runs_of_live_since_publish` counts non-test runs of the live number; `parked_runs` never count as finished/failed) is the derived truth surface — the pblang retirement in this phase must keep `_dry_run`'s `done → simulated` mapping until the v1 runner is gone, then delete it with the runner. luna side: `tests/007.009-vibe-playbook/test_tools.py::test_dry_run_tool` (`status == "done"`, :104) went red with this phase beside the pre-existing `test_propose_accepts_valid_definition` — both are luna/04's (the 007.009 suite pins the v1 contract).
2026-09-08 (phase 10 landed, stamp **0.54.0**; `phases/10-canvas/execution_summary.md`): the canvas check of a migrated playbook is a REST check first — `GET /playbooks/{name}/graph?version=<candidate>` must list `step-<id>` for every call site of the rewritten python code (ids follow the checker: `_id=` → assignment target → literal tool/playbook name → kind, `_2` on duplicates; containers `if-/for-/while-/try-<first inner site id>`, `gather-<first argument id>`; no id derives from a line number, so a compute-only edit keeps every id) and, after the real candidate run, `GET /playbooks/runs/{run_id}.trace` lands one row per journal entry on those nodes (`occurrence` counts loop iterations; a pblang run has no `trace` key). The graph endpoint answers 409 for the pblang live version — the migration's before/after look is python candidate on `V2Canvas` beside pblang live on the client-side canvas, selected per VERSION row (`get_version.format`). The owner visual check on a `vaselin-*` agent (phase 10 Step 10) was deferred to M6 — it happens on the same side-load as this phase's owner publish step, not before.
2026-09-08 (phase 11 landed, stamp **0.55.0** ×3 — THIS phase takes **0.56.0**; part a `aa17ef1`, part b code commit named in `phases/11-delegation-v2-and-keyhole-gate/execution_summary.md`; the keyhole gate verdict is dojop/02's and is appended there): author values this phase will see on rows — every `PlaybookVersion.author` is `agent` (an inline `playbook_propose` / `playbook_edit` / `playbook_manifest_set` write), `delegation:<uuid>` (47 chars; the same three handlers called inside `_drive_delegation`'s `run_turn` chain — `plugin_playbooks/delegation.py::writer_identity()` over the `_delegation_id` ContextVar), `owner` (routes.py `update_playbook` / `put_manifest`) or the pre-phase default; migrated rows written by the agent's rewrite carry `agent`, rows a delegated migration job writes carry `delegation:<id>` — read them via `playbook_overview.candidate.author` / `versions[].author`, `playbook_versions`, REST `list_versions`; the versions tab renders `agent` / `you` / `system` / `delegation <8 chars>` (full id in the span's `title`, testid `version-author-<n>`). Saving over ANOTHER author's unpublished candidate is refused before the ticket is consumed (`versioning.candidate_conflict`; refusal `{stage: "write", saved: false, error, conflict: {candidate_version, author, saved_at}, ticket, ticket_still_valid: true}`; `playbook_edit(replace_candidate=true)` is the owner-authorised way through, row message `candidate (replaced <author> v<N> on owner instruction)`) — a migration job that finds a foreign candidate on a pblang playbook must ask the owner, not replace it. The end-to-end harness this phase can reuse: `tests/test_v2_end_to_end.py` — `_env(tmp_path, decision=)` (v2harness on a FILE-backed sqlite, a real `PlaybookRunner` with the scripted `code_run` fake of `tests/test_v2_loop.py` + a recording `file_write` fake, the lifecycle repro's `_Ctx`/`_Approvals`), `ScriptedAgent(env, did).turn(steps)` (real handlers under `_delegation_id`, `(name, result)` recorded), `_story` = propose(python) → dry run (`simulated`, zero rows) → `playbook_run_candidate(wait_seconds=30)` (`done`, one `is_test` row, the fake called with the real args) → `playbook_status` → `playbook_publish` (one card; the live row keeps the delegation stamp because publish moves the pointer, mints nothing); run-shaped results open `kind, side_effects, version, version_role, run_id`; propose returns exactly `playbook_id, name, format, status, live_version, candidate_version, runnable_via, triggers_active, publish_required, validated, warnings, next`; publish returns `playbook, status, live_version, previous_live_version, gates, evidence, note, next` (no `candidate_version` key — read the pointer from the DB or `playbook_overview`). Canvas step server-side: `GET /playbooks/{name}/graph?version=` `node_ids` ⊇ `step-<id>` per checker call site; `GET /playbooks/runs/{id}` `trace[-1].node` = the last executed site (`failed_line` `null` on a done run). Phase 11 Step 10 (side-load, `author: delegation:<id>` on a `vaselin-*` build, the v1 regression-gate run) DEFERRED to M6 — this phase's owner publish step shares that side-load.

Unblocks: luna-fixer M6 → luna/04 (marketplace publish, re-pin, build, promote, verify). Nothing in this repo ships in this phase.

## Goal

Every live pblang playbook on the target agent is rewritten as a v2 (python-format) playbook by the agent, proven against its own last green live run by a dry run whose effects match, run once for real as a candidate, and published by the owner — one card per playbook — with a ledger row per playbook; the count at start and the final Success-criteria verdict (dojop/03) are recorded; the retirement of the v1 runner and `LANGUAGE_CHEATSHEET` is handed to the owner as a separate decision.

## Scope — changes

1. **Comparison helper** — new module `plugin_playbooks/v2/migrate.py` (an addition to the repo plan's proposed `v2/` layout; recorded in the summary):
   - `v1_effects(definition, rows) -> list[Effect]`: the effect sequence of a recorded **v1** run from its `playbook_step_runs` rows (`PlaybookStepRun` models.py:180-200: `step_id` :187, `step_kind` :188, `status` :189, `inputs` :190, `outputs` :191, `started_at` :195), ordered by `started_at` then primary key. One row exists per executed step, one per loop iteration (`_execute_step` runner.py:641-671 → `_create_step_run` :1360-1371 with `step_kind=step.kind.value` :1365; loop rows per iteration per the `_record_step_cost` docstring :1385-1387). Mapping from `StepKind` (definition.py:15-29): `tool_call` → `tool` (name = `outputs["tool"]`, args = `inputs` — the rendered args stored at :820/:664 with vault refs raw, :767-768), `llm_step` → `llm` and `agent_step` → `agent` (args = `{"prompt": inputs["prompt"]}`, stored truncated to 2000 chars at :1004 / :958), `wait_for_approval` → `approve`, `wait_for_event` → `wait_event` (name = the event), `subtask` → `subtask` (name = the playbook); `code`, `condition`, `parallel`, `loop`, `state`, `halt` contribute no effect (their children have their own rows). The n-th row of a step id becomes occurrence `<id>#<n>`. Rows of steps nested in a `parallel` container (resolved from `definition`) form an unordered group.
   - `v2_effects(journal) -> list[Effect]`: the same sequence from a dry run's `journal` (phase 05 result shape; entries `{kind, id, occurrence, args, ...}` per phase 06's `DbJournalStore.read` shape), skipping entry 0 (`kind="run"`) and the `now` / `random` / `log` kinds; `gather` children are individual effects, grouped unordered.
   - `compare_effects(v1, v2) -> {"match": bool, "v1_count": int, "v2_count": int, "mismatches": [...]}`. **Definition of "same effects with the same args"**: equal length, and at every position the same `kind`, the same `name` (tool name for `tool`, event for `wait_event`, playbook for `subtask`, none for `llm`/`agent`/`approve`) and equal canonical args, where canonical = `json.dumps(args, sort_keys=True, default=str)` after (a) dropping underscore-prefixed effect options (`_id`, timeout/retry options as phase 02/03 define them), (b) keeping `vault:<name>` strings literal (`_VAULT_REF_RE` runner.py:159; never resolved), (c) for `llm`/`agent` comparing only `prompt`, cut to 2000 chars on both sides. **Mismatch classes**, each with `position`, both sides, and for `args` the JSON-pointer paths that differ: `order` (same multiset, different sequence — checked first, so a reorder is one finding, not a cascade), `missing` (v1 effect with no v2 counterpart), `extra` (v2 effect beyond v1), `kind`, `name`, `args`. Any mismatch ⇒ `match: false`. Unordered groups compare as multisets.
   - `require_green_live_run(run_status, is_test, playbook_version, live_version) -> None`: raises `ValueError` naming the Scope 3 rule when the compared run is not `status == "done"`, is a test run (`PlaybookRun.is_test` models.py:165; trigger `agent-candidate`, agent_tools.py:2699) or is not a run of the live version. Stub *derivation* from a v1 run's step rows is NOT this phase's: plugin/08 Scope ("`stubs_from_run` on `playbook_dry_run`") already registers, for a pblang run, every `PlaybookStepRun` row in `started_at` order under both `"<step_id>#<n>"` and `"<tool>#<n>"` with `outputs["result"]` for tool rows (the wrapper written at runner.py:878) and `outputs` otherwise (llm/agent dicts, or `{"_raw": ...}` :975 / :1018), and reports `stubs_source.occurrences_used` / `occurrences_unmatched` (plugin/08 `tests/test_v2_stubs_from_run.py::test_pblang_run_rows_feed_a_python_candidate`). This phase consumes that; it adds no second derivation. 2026-09-08 (phase 08 landed): derivation lives in `agent_tools.py::_stubs_from_recorded_run(session, runner, playbook, run_id, target_fmt) -> (stubs, source) | str(error)`; a python source reads the journal via `runner._v2.journal.read(str(run_id))` (entry 0 = inputs; effect entries `id`, `occurrence`, `kind`, `status`, `result`, `error {type, message}`); `compare=true` must read the compared run's `format` from the run row (`PlaybookRun.format`), never from `Playbook.format`.
2. **Tool option** — `playbook_dry_run` (`_dry_run` agent_tools.py:1572-1633 as extended by plugin/05 and plugin/08) gains `compare: boolean` (default false). It requires `stubs_from_run` (error otherwise). The stubs and the pblang-vs-python dispatch on the compared run come from plugin/08 (per-run `format` column); with `compare=true` `_dry_run` additionally loads the compared run's step rows, applies `require_green_live_run` (error string, no traceback), and the result gains `"comparison": compare_effects(v1_effects(<v1 definition>, rows), v2_effects(result["journal"]))` plus `"compared_run_id"`, `"compared_run_version"`. Dry runs still write no rows (phase 05 invariant). The parameter does not change tool count, policy or risk level (`tests/test_manifest_drift.py:50-60`; `luna-plugin.toml` `[[tools]]` entries carry only name/description/policy/risk_level), so no manifest change.
3. **The "last green live run" rule** (used by the helper and the ledger): the newest `playbook_runs` row of the playbook with `status == "done"`, `is_test` false, `playbook_version == live_version` (stamped at runner.py:416-418; `live_version` models.py:53), trigger any but `agent-candidate`. Found with `playbook_runs(name, version=<live>, status="done")` (`_runs_read` agent_tools.py:1369-1428, ToolDef :1432, returns `is_test`) or `GET /playbooks/{name}/runs?version=<live>` (routes.py:965-990); its rows from `GET /playbooks/runs/{run_id}` (routes.py:993-1024, ordered by `started_at` :1003 — the `playbook_status` tool's step query, `_status` agent_tools.py:612, `select(PlaybookStepRun)` :618-620, does not order, so the helper orders itself).
4. **Migration ledger** — a markdown table in this phase's `execution_summary.md`, one row per live pblang playbook on the target agent: playbook · agent · v1 live version · last green live run id + date · v2 candidate version · dry-run comparison (`match` or the mismatch classes, helper output attached) · candidate run id + status · publish card id (`approval_id` in the `playbook_publish` result, `tests/test_repro_fixplaybooks_lifecycle.py:163`) · resulting live version (read back from `GET /playbooks/{name}` `live_version` routes.py:714) · outcome (`migrated` | `stays pblang: <reason>`).
5. **Where the migration runs** — the plugin is a branch build until M6, so fleet playbooks cannot be migrated in this phase (master §4). Assumption, owner to confirm (repo `PLAN.md` Risks 9; M5 "Decision flagged"): rehearsal on one `vaselin-*` agent with the phase-12 build side-loaded, then the real per-playbook fleet migration after M6 under the same owner approval, one owner publish per playbook, using the same recipe and ledger. Two rehearsal routes: **A** — a `vaselin-*` agent that already holds live pblang playbooks with green live runs (vaselin-scanny-2 has such history, `tests/test_repro_fixplaybooks_lifecycle.py:5-11`); **B** — copies of the fleet's live pblang playbooks, which needs an export/import path that does not exist at HEAD (routes.py has no export/import/bulk route; `POST /playbooks` :721-766 accepts a `definition` only, `PlaybookCreate` :538-544, creates it live as v1 :757 and copies no runs, so the "last green live run" evidence would be missing). Route B therefore means building `GET /playbooks/{name}/export` (playbook row, live version content, last green live run with its step rows — vault refs raw, no secrets) and `POST /playbooks/import` (creates the playbook, inserts the run + rows with `trigger="imported"`, `is_test` false) — proposed shape, built only if the owner chooses B.
6. **Final measurement** — dojop/03 runs the Success criteria against this phase's build; this phase records the verdict table verbatim and the `VERDICT` line, and hands the retirement of the v1 runner + `LANGUAGE_CHEATSHEET` (reference.py, `playbook_language_reference` agent_tools.py:1554) to a separate owner decision with the remaining-pblang count.
7. Tests: new `tests/test_v2_migration.py`; no other test file changes. Version stamps: no bump by default (a new parameter and a module change neither manifest nor UI — repo `PLAN.md` "Conventions"); if the owner picks route B the two routes are not tools either, so still no bump; the number the side-loaded build carries is recorded in the summary and must be above the managed 0.46.0 copy (dojoP `PLAN.md` "01" step 4).

## Not in this phase

- A pblang → python transpiler (master §3 P5: cut). Retiring the v1 runner, `runner.py` semantics, or `LANGUAGE_CHEATSHEET` — owner decision after this phase.
- Marketplace publish, `plugin-set.toml` pin, image build, promotion, verification (M6 / luna/04); pushing any branch.
- The fleet migration itself (after M6, same approval); creating agents other than `vaselin-*`.
- dojoP grader/task changes (dojop/03 owns `no_redundant_validate`, `no_validate_errors`, the run and the table).
- Any of the seven red repro tests: this phase turns none green; the expected red set is whatever plugin/11's summary left (the three lifecycle pins belong to the P0 plans).
- UI changes (the comparison is a tool/JSON surface only); vault/credential handling; `ctx.sleep`; any change to plugin/08's `stubs_from_run` (journal or pblang-row path) or its `stubs_source` shape — this phase only reads them (2026-09-08, phase 08: shape frozen as `{run_id, version, format, status, occurrences_used, occurrences_unmatched}`; `trace["stubs_source"]` is set only when `stubs_from_run` was accepted).

## Steps

1. Preconditions. Read plugin/11's summary (suite green, dojop/02 `VERDICT: go`) and plugin/08's summary (`stubs_from_run` shape, journal entry fields). Record HEAD. Reconcile the uncommitted `uv.lock` (repo `PLAN.md` Risks 5) without installing anything. Proof: the summary quotes both verdict lines and HEAD before.
2. Write `plugin_playbooks/v2/migrate.py` (Scope 1) with `Effect` as a small dataclass `(kind, name, args, occurrence, group)` and the four functions; pure, no DB access — callers pass rows/journal lists. Proof: `tests/test_v2_migration.py` unit tests (Exit tests 1-6) green.
3. Wire Scope 2 into `_dry_run`: after plugin/08's `stubs_from_run` resolution (which already refuses a run of another playbook), load the compared run's step rows and the v1 definition (the version row of `compared_run_version`) in the same session that resolves the version; refuse (error string, no traceback) when `compare=true` without `stubs_from_run` or when `require_green_live_run` raises. Proof: Exit test 7 green; `tests/test_manifest_drift.py` green with the tool count unchanged.
4. Record the count at start on the target agent named by the owner: `GET /playbooks` (routes.py:637-681; the default `status=active` filter :641-642 is exactly `enabled` + `disabled`, archived excluded) with the `format` field plugin/04 adds to the list entries (:674) and to `GET /playbooks/{name}` (:699-718) — `Playbook.format` is the live format until publish (plugin/08 `test_python_candidate_beside_pblang_live_runs_each_under_its_runtime`). At HEAD neither route exposes `format`; if plugin/04's field is absent on the build, sniff `code` from `GET /playbooks/{name}` :684-718 with plugin/01's `sniff_format`. Count = playbooks with status `enabled` or `disabled` whose live version is pblang. Proof: the number, the per-playbook list (name, status, live_version, last green live run id or `none`), and the exact source (route/tool + agent) in the summary.
5. Side-load the phase-12 build on the rehearsal `vaselin-*` agent the way plugins 05/10 did (their summaries record the command); run one existing v1 playbook for real. Proof: the agent's plugin version equals the branch stamp; the v1 run is `done` (standing regression gate, master §4).
6. Per playbook, one at a time, no bulk actions (M5 owner touchpoints):
   a. The agent rewrites it with the v2 skill through `playbook_edit` (read → ticket → write; candidate, python format). Migration rule: every v1 step id is kept as the v2 effect `_id`, so occurrences align (`s1#1` ↔ row 1 of `s1`). Proof: `candidate_version` set, format python.
   b. `playbook_dry_run(name, version="candidate", stubs_from_run=<last green live run>, compare=true)`. Pass = `comparison.match == true` and `unreached_call_sites == []` (a non-empty list is allowed only with a written reason — e.g. a branch the recorded run never took). At most three rewrite iterations; then the row records `stays pblang: dry-run mismatch <classes>` and the candidate is left unpublished (M5 risk 2).
   c. `playbook_run_candidate(name, inputs=<the recorded run's inputs>)` (`_runs_read` returns `inputs`, agent_tools.py:1399). The owner confirms per playbook that replaying those inputs is acceptable — the run performs the real side effects — or supplies inputs; a parked `approve`/`wait_event` (plugin/07) is answered by the owner. Proof: run `done`, `is_test` true, version = candidate.
   d. `playbook_publish(name, explanation=...)` → one card → owner approves. Read back `live_version` from `GET /playbooks/{name}` (routes.py:714) — by hand, since the publish-verify P0 plan's `verified` key may not have shipped. Proof: `live_version == candidate version`, `candidate_version` null.
   e. Ledger row (Scope 4) with the helper output saved as `evidence/<playbook>.dry-run-comparison.json` next to the summary (raw vault refs only — check the file for `vault:` values and nothing resolved before committing).
7. Suite, drift and "v1 untouched" checks (Exit tests 9-11); commit locally on `v2-runtime` ("032 phase 12: migration helper + ledger"); no push.
8. Hand the build to dojop/03 (dojoP `phases/03-final-measurement/PLAN.md` Steps 4-5: `LUNA_PLUGIN_SET_DIR=<scratch>/plugin-set tools/hermetic.sh --luna-repo … -- --trials 5 --ids <the 10 live tasks>` — `--ids`, not `--tags fix-playbooks`, because the v1 twins keep that tag; the repo-level "03" step 5 wording with `--tags` is superseded by the phase file; alternative target: the `vaselin-*` agent of Steps 5 via `run.py run --base <url> --token <token>`, token never printed; version proof `run.json.plugin_versions["plugin-playbooks"]` = the branch stamp and `plugin_upgrades.upgraded` without plugin-playbooks). Ordering: this phase's build (steps 2-7) → dojop/03 run → this phase's summary; the phase-level circularity (032 index: 12 depends on dojop/03; dojoP: 03 depends on the plugin/12 build) resolves at step level. Proof: `results/00NN-run` id and the per-criterion table copied verbatim.
9. Write the summary: count, ledger, dojop/03 table + `VERDICT`, the retirement handoff line ("v1 runner + `LANGUAGE_CHEATSHEET` retirement: owner decision pending; pblang playbooks remaining on <agent>: N"), the fleet note (fleet count taken at fleet-migration time, M5 risk 1), and the version M6 must publish and pin (`luna/plugin-set.toml:62-65`; the repo `PLAN.md` cites :63-65 — same block). A `fail` row ⇒ `VERDICT: stop`; M6 waits (master §3 Success criteria stop rule).

## Exit tests

All in `tests/test_v2_migration.py` unless stated. `tests/conftest.py` only installs the fake `luna_sdk` (no engine or tool fixture — repo `PLAN.md` "Conventions used"); each test builds its own `create_async_engine("sqlite+aiosqlite://")` + `Base.metadata.create_all` and `build_tools(sf, _Bus(), <runner>, ctx)` as `tests/test_repro_fixplaybooks_lifecycle.py::_env` (:99-105) does, uses `evidence.green_run(sf, version, is_test=False)` (`tests/evidence.py:23-38`; `is_test=False` stamps trigger `schedule`) for live-run rows and inserts `PlaybookStepRun` rows directly. Tests 1-6 are pure (no jail). Test 7 drives the v2 dry run through the segment loop: it uses plugin/05's dry-run harness (`tests/_jail.py::real_code_run` under `pytest.mark.real_jail`, skipped without the managed install — plugin/02 Step 4, repo `PLAN.md` Risks 6 — or the scripted `code_run` fake if plugin/05's `tests/test_v2_dry_run.py` runs on one; whichever plugin/05's summary records).

1. `test_v1_effects_maps_every_step_kind` — rows covering all twelve `StepKind` values yield exactly `[tool, llm, agent, approve, wait_event, subtask]` effects; two rows of `s1` become `s1#1`, `s1#2`; a `parallel` container's children carry the same `group`.
2. `test_compare_match_on_identical_sequences` — `compare_effects(v1, v2)["match"] is True` and `mismatches == []`.
3. `test_compare_reports_each_mismatch_class` — parametrized over `order`, `missing`, `extra`, `kind`, `name`, `args`: exactly one finding of that class at the expected `position`; the `args` finding lists the JSON-pointer path and both values.
4. `test_compare_keeps_vault_refs_raw_and_drops_effect_options` — `{"headers": {"x-api-key": "vault:acme"}, "_id": "s1"}` vs the same without `_id` → `match`; the helper output contains `vault:acme` and no other value for that key.
5. `test_compare_llm_prompt_prefix_only` — an llm effect whose v2 prompt equals the v1 prompt beyond the 2000-char cut matches; a differing prefix is an `args` finding on `/prompt`.
6. `test_require_green_live_run_refuses_non_green` — parametrized: a `failed` run, an `is_test` run and a `done` run of a non-live version each raise `ValueError` whose message names the Scope 3 rule; a `done`, non-test, live-version run passes.
7. `test_dry_run_compare_option_end_to_end` — seeded playbook with a pblang live version, a green live run with rows (`green_run(..., is_test=False)` + `PlaybookStepRun` rows whose `step_id`s the candidate reuses as `_id`), and a python candidate: `playbook_dry_run(name=..., version="candidate", stubs_from_run=<run_id>, compare=True)` returns `comparison.match is True`, `steps_ran` keys equal the v1 occurrence keys (`stubs_source.occurrences_unmatched == []`), `compared_run_id` echoed; a candidate with a changed tool arg returns `match False` and `mismatches[0]["class"] == "args"`; `compare=True` without `stubs_from_run` returns an `error`; `compare=True` against an `is_test` run returns an `error` naming the rule; the `playbook_runs` count is unchanged after every call (no rows written).
8. `tests/test_manifest_drift.py` green: tool set unchanged (`:50-60`), tables unchanged (`:63-66`), stamps agree (`:69-78`).
9. Existing suite green at plugin/11's count plus the new file; repro tests unchanged (none flip in this phase; list the red set in the summary).
10. v1 runner untouched: `git diff <plugin/11 HEAD>..HEAD --stat -- plugin_playbooks/runner.py plugin_playbooks/reference.py plugin_playbooks/definition.py` is empty.
11. Ledger and measurement (checked by reading the summary): the count line names its source; every ledger row is complete — for `migrated` rows the dry-run comparison is `match` with the evidence file present, the candidate run is `done`, the card id is recorded, the live version read back equals the candidate version; for `stays pblang` rows the reason is one of `no-evidence` (no green live run of the live version), `dry-run mismatch <classes>`, `candidate run failed <run id>`, `owner declined <card id>`; the dojop/03 table has all eight criterion rows (pass^k ≥ 0.9 on both twins; `authoring-within-budget` green; edit turn ≤ 3 tool calls; zero validate errors; zero redundant validates; keyhole 7 ≥ 1.00; zero honesty / `must_not_call` violations; ≥ 5 trials per task) with the version proof and the `results/00NN-run` id; the retirement handoff line is present.

## Cross-repo checks

- **dojop/03** (dojoP `plans/0002-fix-playbooks-bench/PLAN.md` "03 — Final measurement" and `phases/03-final-measurement/PLAN.md` @ 316f007): it depends on "plugin/12 steps 2-7" and measures that build, so steps 2-7 complete before its run; the side-loaded manifest version must be strictly above the managed 0.46.0 copy (dojoP "01" step 4) and a run whose `plugin_versions` does not prove the branch build is discarded (dojoP "Constraints"); its Step 10 pastes the FINAL MEASUREMENT block into this phase's `execution_summary.md` and luna-fixer M5's, and its Exit test 5 requires `diff` of the block across the three files to be empty — Steps 9 here accepts that block verbatim and adds nothing inside it; `VERDICT: stop` blocks M6.
- **plugin/08** (`stubs_from_run`): the master's `stubs_from_run` reads a recorded run's *journal*; plugin/08 Scope already covers the journal-less case — a pblang run's `PlaybookStepRun` rows in `started_at` order register `"<step_id>#<n>"` and `"<tool>#<n>"`, tool rows contribute `outputs["result"]`, and the result carries `stubs_source` (plugin/08 Risks 3, `test_pblang_run_rows_feed_a_python_candidate`). This phase adds only `compare`. Plugin/08's Cross-repo line says "dojop/03's migration helper consumes `stubs_source`" — the comparison helper is plugin/12's per the phase index (dojop/03 "Not in this phase" excludes the migration and ledger); record the correction in this summary, plugin/08's file is not edited here. The dry-run result's `journal` entries carry `seq, kind, id, occurrence, args, …` (plugin/06 `DbJournalStore.read`; the tool name must be inside `args` or a sibling field — confirm from `docs/v2.md` §6 at execution); per-occurrence keys `<id>#<n>`, `steps_ran` and `unreached_call_sites` are consumed as plugin/05 defined them (`result["journal"]`, `result["steps_ran"]`). Format-changing edits: `version="candidate"` (python) with `stubs_from_run` naming a pblang-version run dispatches the v2 loop for the candidate and `playbook_publish` promotes a python candidate over a pblang live version — plugin/08's `tests/test_v2_format_rows.py::test_python_candidate_beside_pblang_live_runs_each_under_its_runtime` and `test_edit_may_change_format` cover this pair; Exit test 7 relies on them.
- **luna-fixer M6 / luna/04**: nothing ships here. luna `plans/107-fix-playbooks/PLAN.md` phase 04 step 1 says "plugin/12 has published plugin-playbooks vX" — the master §4 "Ship" and M6 step 2 put the marketplace publish in M6 after the owner's final-state approval, so plugin/12 only records the version to publish; master wins, the luna/04 wording is a correction for M5 step 4 to record. The version this phase's build carries is the one M6 publishes and pins over 0.39.0 (`luna/plugin-set.toml:62-65`).

## Risks and open questions

1. **Rehearsal route (owner decision)**: A (a `vaselin-*` agent with its own live pblang playbooks and run history) needs no new code; B (copies of fleet playbooks) needs the export/import pair in Scope 5 because no such route exists at HEAD and `POST /playbooks` copies no runs. The fleet migration happens after M6 under the same approval; its count is taken then.
2. **`stubs_from_run` on a v1 run** is plugin/08's assumption (its Risks 3), not the master's text (which speaks of a journal): stub values come from step-row `outputs` (`{"tool","result"}` wrapper at runner.py:878; llm/agent dicts at :975/:1018). If plugin/08's summary shows it shipped without the pblang-row path, that path is a plugin/08 revision, not this phase's scope; this phase stops and records it (repo `PLAN.md` "Stop rules").
3. **What "same effects with the same args" compares**: effects are `tool/llm/agent/approve/wait_event/subtask` only (`now`/`random`/`log`/compute excluded); llm/agent args are the prompt prefix (v1 stored 2000 chars, no schema); underscore options are ignored; the rewrite keeps v1 step ids as effect ids so occurrences align. A rewrite that legitimately reorders independent effects is an `order` mismatch and stays pblang unless the owner accepts the row with the finding attached (recorded, not silently passed).
4. **v1 `wait_for_approval` auto-approves (runner.py:1060-1069) and `wait_for_event` returns a stub immediately (:1071-1083)**: a v1 "green" run carries no real decision or event payload; the comparison holds (args, not results), but the real candidate run parks on these effects and the owner answers them; noted per playbook in the ledger.
5. **Count definition**: "live" = status `enabled` or `disabled` with a pblang live version (archived excluded), per target agent (M5 risk 1). The list route exposes no format at HEAD (routes.py:637-681); plugin/04 adds `format` to its entries (:674) — the source used is recorded verbatim.
6. **No-evidence playbooks**: a live pblang playbook with no green live run of its current live version cannot be checked by the recipe; it stays pblang with reason `no-evidence` unless the owner runs it once for real first (side effects) — owner's call per playbook.
7. **Replaying recorded inputs** in the candidate run repeats the run's side effects (emails, tickets, chat messages); the owner confirms inputs per playbook. Vault refs in rows and evidence files stay `vault:<name>` (runner.py:767-768); credentials referenced by migrated playbooks must exist on the rehearsal agent for the candidate run — never printed or committed.
8. **Phase ordering with dojop/03** is circular at phase granularity; resolved as build → dojop/03 run → summary (Steps 8).
9. **Publish read-back**: the publish-verify P0 plan (`test_publish_success_carries_verified_readback`) may still be red; the ledger's live version is read from the row, not the publish result.
10. **Version stamp**: no bump assumed (no manifest/UI change); if route B adds routes only, still none; if the executor adds a tool instead of a parameter, bump the minor and regenerate `luna-plugin.toml`. The measured build's stamp is the ship version candidate — fixed at M6.
11. **Parallel groups**: v1 rows of parallel branches interleave by `started_at`; the helper needs the v1 `definition` (from the version row, `GET /playbooks/{name}/versions/{n}` routes.py:1116) to know container membership; v2 `gather` children are assumed journaled in start order and compared as a multiset.

## Execution summary

Written to execution_summary.md in this folder after the phase runs, using this template:
- Ran: (commands, dates, HEAD before/after, results folder ids)
- Results: (every exit test with its outcome and the relevant output; anything red and why)
- Deviations from this plan: (what changed and why)
- Learned: (facts that change later phases)
- Revised: (which later phase files were edited because of this, and how)

## Post-M4 checklist (2026-09-08, luna-fixer M4 gate GREEN at the third evaluation — `plans/2026-09-06-fix-playbooks/phases/M4-truth-surface-canvas-delegation/execution_summary.md`)
- Starting point: `v2-runtime` @ `7e3c8d1` (docs: phase-11 summary + `Status: done`) over code `0ca8b5f` = plugin/11 part b; stamps **0.55.0** ×3 (`pyproject.toml:3`, `plugin_playbooks/luna-plugin.toml:2`, `plugin_playbooks/__init__.py:728`); manifest 26 tools / 11 tables / 3 skills. **This phase takes 0.56.0** (the Scope 7 "no bump by default" line is superseded by M4 operator decision 4: the 0.53.0-0.55.0 stamps are bumps on the branch and the measured build's stamp is the ship candidate — bump the three lines, regenerate nothing unless a tool is added, `tests/test_manifest_drift.py::test_version_stamps_agree` pins them). Managed copy `~/.luna/managed_plugins/plugin_playbooks` is 0.46.0 — 0.56.0 stays strictly above it. ` M uv.lock` pre-existing: never staged, never discarded.
- Suite baseline: `7 failed, 713 passed` (720 collected, ~83 s, `.venv/bin/python -m pytest -p no:cacheprovider -q -rfEsx tests`). The 7 red = the repro pins by name (`tests/test_repro_fixplaybooks_lifecycle.py::{test_publish_success_carries_verified_readback, test_approved_then_regated_same_payload_trips_loop_guard, test_manifest_set_does_not_flip_live}`, `tests/test_repro_fixplaybooks_runtime.py::{test_interrupted_run_survives_restart_instead_of_failing, test_wait_for_approval_actually_gates, test_wait_for_event_actually_waits, test_tool_step_timeout_is_enforced}`); flip none, never skip/xfail, never weaken; set at exit = set at entry (Exit test 9 reads "plugin/11's count" = 713 + 7). Three known load flakes (green alone; none fired at the gate): `tests/test_v2_parked.py::test_wait_event_deadline_passed_during_downtime`, `tests/test_delegation.py::test_slow_path_returns_running_then_status_polls_done`, `tests/test_wake_on_completion.py::test_slow_run_promises_wake_and_stamps_flag`. `npm test` baseline 20 files / 150 (plugin/11) — this phase has no UI change (Not in this phase), so `npm test` is not required unless the bundle is touched.
- Rules: relative imports only + `tests/test_loader_style_import.py` (2) green before every commit; multi-run-task tests use the file-backed `db_file(tmp_path)` fixture (`tests/test_v2_resume.py`), never the in-memory `StaticPool` engine, no DB reads under a live run task; `tests/test_v2_parity.py` (18) and `tests/test_v2_live_version_invariant.py` (2) stay green (a new `live_version` writer goes on `KNOWN_SIDE_DOORS` by symbol, never by loosening the scan); ruff F401 clean; commits local only on `v2-runtime`, never push, never publish; no `vaselin-*` machine unless the owner chooses the rehearsal route (Risks 1) — every earlier side-load (plugin/09-11 step 10, the M4 owner visual check, the PG proof) is deferred to M6 and this phase's Step 5 shares that side-load.
- Gate facts this phase cites in Step 1 (Preconditions): dojop/02's `VERDICT: go` is dojop/02b's — dojoP `results/0066-run` (7 keyhole tasks, `--trials 2`, 14/14, failed code checks 0, `must_not_call` 0, wrong-claim 0, judge advisory 31/34) + `results/0064-run` (`--trials 5`: queue-v3 5/5, editing-v3 5/5, within-budget 5/5, edit-turn max 2, validate errors 0, redundant validates 0) + `results/0063-run` (`candidate-then-publish` 2/2), all on 0.55.0 @ `cf3274d` side-loaded into the local hermetic server; the block is in this repo's `phases/11-delegation-v2-and-keyhole-gate/execution_summary.md` "Keyhole gate (dojop/02b re-run)". The 0062 stop was a task-contract mismatch resolved by a dojoP task re-pin (dojoP `b3957ad`: `playbook_run_candidate` accepted on the "really run it" turns; operator decision, owner may overrule — no plugin change was needed; `agent_tools.py:884-895`'s refusal of `playbook_run` on a candidate-only row with `runnable_via: playbook_run_candidate` is the contract dojop/03 measures against).
- What M4 landed that this phase consumes (beyond the dated "Depends on" lines above): plugin/09 envelope on every run-shaped result (`kind` from the ROW; a migrated playbook's candidate run is `candidate_test_run`, `version_role: candidate`; the ledger's "candidate run id + status" and "resulting live version" read `playbook_overview(name)` — `candidate {version, author, saved_at, last_test_run}`, `runs_of_live_since_publish`, `versions[]` with `live` / `candidate` flags — rather than the publish result, which has no `candidate_version` key); plugin/10 `GET /playbooks/{name}/graph?version=<candidate>` for the canvas check of each migrated playbook (`node_ids` ⊇ `step-<id>` per call site; 409 on the pblang live version) + `get_run.trace` after the real candidate run; plugin/11 author values (`agent` for the rewrite, `delegation:<uuid>` if a delegated job writes) and the conflict guard (a foreign unpublished candidate on a pblang playbook is REFUSED — ask the owner; `playbook_edit(replace_candidate=true)` only on owner instruction); luna/03 tool groups (`playbook_overview` is in the automation group, `tool_groups.py:132`; hidden in the bench until the group loads).
- dojop/03 hand-off (its Step 5 `--ids` list of 10 live tasks, ≥ 5 trials): the keyhole files it measures are the dojop/02b re-pinned ones; its judge must register `candidate_not_claimed_live` / `shipped_or_honest` in `lib/judge.py DIMENSIONS` (4 noise misses in 0063); residual risk it inherits: `keyhole-edit-not-in-run` t4 is agent-choice nondeterministic (owner question in the luna-fixer M4 summary, decision 15). Its FINAL MEASUREMENT block is pasted verbatim into this phase's summary (Step 8-9) and luna-fixer M5's.
- luna side for M6 / luna/04 (record, do not act): luna `fix-playbooks` @ `031d9d3` (code `76bd504`, 0.92.045; luna/04 ≥ 0.92.046); the two 007.009 v1 pins luna/04 re-pins (`tests/007.009-vibe-playbook/test_tools.py:71` `created → candidate_saved`, `:104` `done → simulated`); the version this phase's build carries (0.56.0 unless changed) is the one M6 publishes and pins over 0.39.0 (`luna/plugin-set.toml:62-65`).
